# مراجعة تحذيرات Supabase — 12 سبتمبر 2026

المصدر: تصدير Security Advisor الذي قدّمه صاحب المشروع، بتاريخ رصد `2026-09-12T11:57:18.990Z`. هذه مراجعة مرحلية وليست شهادة أمان أو تدقيقًا كاملًا للتطبيق. طبّق صاحب المشروع إصلاح قوائم التخزين وأرسل نجاح التنفيذ ثم صورة إعادة الفحص: 0 أخطاء و62 تحذيرًا بدلًا من65.

| المجموعة | عدد التحذيرات | الحالة |
|---|---:|---|
| Public Bucket Allows Listing | 3 | طُبّق بواسطة المالك؛ انخفض إجمالي التحذيرات من65 إلى62 بعد إعادة الفحص |
| Public Can Execute SECURITY DEFINER Function | 26 | تحتاج مراجعة محتوى الدوال وصلاحياتها وتبعيات RLS |
| Authenticated Can Execute SECURITY DEFINER Function | 35 | تحتاج مراجعة؛ بعض الدوال متاحة للمستخدمين بقصد تشغيل ميزات التطبيق |
| Leaked Password Protection | 1 | الميزة غير مفعّلة في التصدير، ومتاحة في Pro فما فوق حسب الوثائق الرسمية |

عدد تحذيرات الدوال لا يعني عدد دوال منفصلة؛ قد تظهر الدالة نفسها في مجموعتي الزوار والمسجّلين. لا يجوز سحب صلاحيات كل الدوال أو تغييرها إلى SECURITY INVOKER دفعة واحدة؛ قد تعتمد عليها سياسات الوصول أو وظائف الطلبات والرسائل. يلزم فحص تعريفاتها الفعلية في الإنتاج أيضًا.

## الخطوة الأولى: قوائم ملفات التخزين

الملف: `supabase/migrations/20260912130000_restrict_storage_listing.sql`.

يقيد استعراض سجلات `factory-media` بمالك الملف أو مجلده والمدير، ويقيد استعراض `promotion-media` و`video-promotion-media` بالمدير. يضيف سياسة SELECT مقيّدة تمنع سياسة واسعة أخرى من تجاوز هذه الحدود. تبقى الحاويات عامة وتستمر روابط الصور والفيديو العامة؛ هذا لا يجعل الملفات التي يُعرف رابطها خاصة. تبقى سياسات الكتابة وصلاحيات المحادثات الحالية كما هي.

التطبيق والويب يعرضان الوسائط العامة بروابطها، ولم تُرصد استدعاءات لتعداد Storage في مساراتهما الحالية. يتضمن الاختبار سياسات المصانع الأصلية وترحيلي إعلانات الصور والفيديو الفعليين.

اختبار محلي: `node scripts/test_storage_listing.mjs app_flutter/build/video-ad-validation` بعد تثبيت `@electric-sql/pglite` في المجلد المعزول المشار إليه. اجتاز 8 اختبارات: قابلية إعادة التشغيل وحفظ الملفات وخصائص الحاويات، منع تعداد الزائر، عزل ملفات الأعضاء، صلاحيات المدير، رفع/تعديل/حذف ملفات المصنع، رفع وحذف وسائط الإعلان مع حماية الغلاف المرتبط، ثبات عزل حاوية أخرى، ومنع تجاوز القيد بسياسة SELECT واسعة.

**التطبيق على الإنتاج:** شغّل المالك الملف في SQL Editor وأرسل نجاح التنفيذ وانخفاض العدد بثلاثة بعد إعادة الفحص. لا تتضمن صورة العدد تفاصيل التحذيرات المتبقية. لم يرد تأكيد منفصل لاختبار عرض ورفع وحذف الوسائط على الإنتاج بعد التغيير.

## الخطوة الثانية: مطابقة تعريفات الدوال الفعلية

الاستعلام `supabase/diagnostics/security_functions_review.sql` للفحص فقط. يعرض تعريفات دوال `public` وصلاحيات تشغيلها وسياسات RLS ومشغلاتها، دون استدعاء دوال التطبيق أو قراءة سجلات العملاء. أرسل المالك تصدير النتائج: 37 دالة و62 سياسة و23 مشغلاً. تؤكد سياسات التخزين في التصدير تطبيق إصلاح الخطوة الأولى. ملف التصدير نفسه غير مضاف إلى المستودع.

### إصلاح طُبّق بواسطة المالك

`supabase/migrations/20260912140000_restrict_internal_functions.sql` يعالج نطاقاً محدداً:

- **كشف حالة الطلب:** `has_purchased_product(bigint, uuid)` تقبل معرّف أي مستخدم وتقرأ طلباته بصلاحيات `postgres`، وللزائر والمسجّل صلاحية استدعائها. أُعيد إنتاج كشف Boolean عن وجود طلب في قاعدة معزولة ببيانات وهمية، مع عدم قدرة الزائر على قراءة جدول الطلبات مباشرةً. لم تُجرَ هذه المحاولة على بيانات العملاء الفعلية. الإصلاح يسحب EXECUTE من PUBLIC وanon وauthenticated، ويُبقي الاستدعاء الداخلي من `get_product_reviews` بمالكها المشترك، وبالتالي تستمر شارة `is_verified` للتقييم المنشور.
- **استدعاءات خاصة بالمسجّل:** يسحب PUBLIC وanon من `can_review_product` و`get_message_products`، ويحتفظ بصلاحية authenticated. راجعت مساري Flutter والويب؛ حالة التقييم تُطلب بعد تسجيل الدخول. تتحقق دالة منتجات الرسائل أيضاً من عضوية المحادثة.
- **الدوال الداخلية للمشغلات:** يسحب التنفيذ المباشر من PUBLIC وanon وauthenticated للدوال SECURITY DEFINER التي تعيد trigger/event_trigger داخل public. في التصدير الحالي هي 11 دالة. لا تتغير تعريفاتها ولا المشغلات المربوطة بها. هذه تحذيرات عن صلاحيات زائدة؛ لا يعني ظهورها أن دوال المشغلات كانت قابلة للتشغيل كاستدعاء RPC عادي.
- يحتفظ بصلاحيات دوال RLS مثل `is_admin` و`owns_factory` وبقية دوال الأعمال في هذه المرحلة. يفشل الترحيل ذرياً إذا كانت هناك سياسة تعتمد على الدوال الثلاث المستهدفة أو اختلف مالك دالة التقييم عن مالك دالة فحص الطلب.

اختبار: `node scripts/test_internal_function_permissions.mjs app_flutter/build/video-ad-validation [diagnostic-json]`. اجتاز 12 اختباراً باستخدام التعريفات الفعلية المصدرة محلياً وبيانات وهمية، بما يشمل منع الاستدعاء المباشر، استمرار شارات التقييم، عضوية المحادثة، حماية تغيير صلاحية المدير، استمرار مشغلات التقييم والرسائل وصور الفئات، استمرار مشغل تفعيل RLS، وإيقاف التغيير عند تبعية غير متوقعة. هذه اختبارات للنطاق المعدّل، وليست اختباراً شاملاً لكل مسارات التطبيق.

**الحالة:** أرسل المالك نجاح التنفيذ ثم نتيجة Security Advisor: صفر أخطاء و36 تحذيراً بدلاً من62. يطابق الانخفاض المتوقع بـ26 تحذيراً (22 للمشغلات، و2 لفحص الطلب، و2 لاستدعاءات الزائر). أكد المالك استمرار ظهور التقييمات والبطاقات؛ لم يُجر اختبار شامل لكل مسارات التطبيق على الإنتاج. ما زالت بقية الدوال بحاجة إلى مراجعة.

## الخطوة الثالثة: حماية ارتباط المنتجات بالرسائل

الملف المطبّق بواسطة المالك: `supabase/migrations/20260912150000_secure_message_product_links.sql`.

أُعيد إنتاج مشكلتين باستخدام تعريفات الدوال المصدرة وبيانات وهمية في PostgreSQL معزول: يستطيع طرف محادثة إرسال معرّف منتج من مصنع آخر، ويستطيع مستلم رسالة استبدال معرّف المنتج فيها. كانت `get_message_products` تتحقق من عضوية المحادثة دون مطابقة مصنع المنتج، ولذلك كشفت تفاصيل منتج لمصنع غير معتمد رغم منع قراءته المباشرة بسياسة المنتجات. لم تُجر هذه المحاولات على بيانات الإنتاج.

الإصلاح يتحقق عند الإدراج من المرسل وعضويته ومن انتماء المنتج لمصنع المحادثة، ويمنع تبديل المنتج أو هوية الرسالة وتاريخها عند تحديث حالة القراءة. تتحقق دالة عرض البطاقات أيضاً من مصنع المنتج، فلا تعيد تفاصيل روابط قديمة مخالفة. يحافظ على الرسائل الموجودة، وعلى حذف المنتج عبر `ON DELETE SET NULL`، وعلى عرض بطاقات المحادثات القائمة إذا تغير اعتماد المصنع. تحتفظ الدوال بصلاحيات التنفيذ المقيدة في الخطوة السابقة.

الاختبار: `node scripts/test_message_product_security.mjs app_flutter/build/video-ad-validation [diagnostic-json]`. اجتاز 11 اختباراً بالتعريفات المحلية وبالتعريفات المصدرة، تشمل إعادة إنتاج المشكلتين، تكرار الترحيل، منع كشف الروابط المخالفة القديمة، إرسال البطاقات بواسطة الطرفين، رفض انتحال المرسل والمنتجات الأجنبية، حماية الحقول عند تأشير القراءة، استمرار النصوص والصور والفيديو، عزل المحادثات وصلاحيات التنفيذ، تغير اعتماد المصنع، وحذف المنتج دون حذف الرسائل. اجتازت اختبارات صلاحيات الدوال السابقة الـ12 أيضاً بعد إضافة التغيير إلى ملفات المشروع.

**الحالة:** أرسل المالك صورة نجاح التنفيذ في Supabase، ثم أكد بعد طلب تجربة الرسائل وبطاقات المنتجات: «سليم كل شيء حقيقي والرسائل توصل». بذلك تأكد تطبيق الترحيل وعمل المسار الذي جرّبه المالك على الإنتاج؛ لا يمثل ذلك اختباراً شاملاً لكل الصلاحيات. أكد التصدير اللاحق بقاء العدد 36؛ هذا الإصلاح يتحقق داخل الدوال ولا يغير منح الصلاحيات.

## التصدير المحدّث والخطوة الرابعة: صلاحية مساعد سعر الشرائح

استُلم `Supabase Performance Security Lints (yhofxryhlrrwzztfowpa) (1).csv` بعد تحديث الفحص: 36 تحذيراً، منها 12 لصلاحيات anon و23 لصلاحيات authenticated، وتحذير واحد لحماية كلمات المرور المسرّبة. يتطابق ذلك مع إزالة تحذيرات الخطوتين الأولى والثانية؛ إصلاح ارتباط منتجات الرسائل لا يغير هذه المنح. ليست كل دالة SECURITY DEFINER متاحة للعميل ثغرة، ويجب مراجعة التحقق داخل الدوال وتبعيات سياسات الوصول قبل سحب الصلاحيات.

`tier_unit_price(bigint,numeric)` تقرأ سعر المنتج وشرائح كمياته بصلاحيات مالكها دون فحص إتاحة المنتج للمستخدم. أُعيد إنتاج كشف سعر منتج مخفي للزائر والمستخدم المسجّل ببيانات وهمية في قاعدة معزولة، رغم أن SELECT المباشر لم يُعد المنتج. لم تُقرأ بيانات عملاء الإنتاج.

الملف `supabase/migrations/20260912160000_restrict_tier_price_helper.sql` يسحب الاستدعاء المباشر من PUBLIC وanon وauthenticated. البحث في الويب وFlutter لم يجد استدعاء RPC لهذه الدالة؛ المستدعي الموجود في تعريفات الإنتاج المصدرة هو `create_order_from_cart` بصلاحيات مالك مشترك. يبقى الحساب الداخلي متاحاً. يرفض الترحيل التغيير إذا وجد اعتماداً من سياسة في public/storage، أو اختلف مالك إنشاء الطلب، أو لم يعد إنشاء الطلب SECURITY DEFINER. لا يعدّل أجسام الدوال أو الأسعار أو طريقة الدفع اليدوية المعتمدة.

اجتاز `scripts/test_tier_price_permissions.mjs` سبعة اختبارات باستخدام التعريفات المحلية والتعريفات المصدرة: إعادة إنتاج كشف السعر، تكرار الترحيل وحفظ أجسام الدوال، منع الاستدعاء المباشر، إنشاء طلبات بأسعار الشرائح والكميات الكسرية والسعر الأساسي والسعر الخاص مع التحقق من الإجماليات وتفريغ السلة، اشتراط الهوية وعزل سلة المشتري، رفض اعتماد سياسة غير متوقع، ورفض اختلاف مالك دالة إنشاء الطلب. هذه اختبارات للصلاحية المعدّلة وليست مراجعة كاملة لأمان إنشاء الطلبات.

التشغيل: `node scripts/test_tier_price_permissions.mjs app_flutter/build/video-ad-validation [diagnostic-json]`.

**الحالة:** أرسل المالك نجاح التنفيذ ونتيجة Security Advisor: صفر أخطاء و34 تحذيراً بدلاً من36. ثم أكد صحة إنشاء الطلب والأسعار بعد طلب اختبارهما. أصبحت نتيجة الفحص 34 تحذيراً بدلاً من65 في بداية المراجعة. ما زالت التحذيرات المتبقية تحتاج مراجعة؛ هذه النتائج ليست شهادة أمان شاملة. تُحفظ ملفات الإصلاحات الأربعة واختباراتها واستعلام التشخيص وهذا التقرير معاً في المستودع بطلب المالك، دون تضمين صادرات قاعدة الإنتاج أو سجلات الاختبار المحلية.

مصادر رسمية:
- [الحاويات العامة والخاصة](https://supabase.com/docs/guides/storage/buckets/fundamentals)
- [سياسات الوصول إلى التخزين](https://supabase.com/docs/guides/storage/security/access-control)
- [تحذيرات صلاحيات الدوال](https://github.com/supabase/splinter/blob/main/docs/0029_authenticated_security_definer_function_executable.md)
- [حماية كلمات المرور المسرّبة](https://supabase.com/docs/guides/auth/password-security)
