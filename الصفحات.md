# دليل صفحات المشروع

> **القاعدة في سطر واحد:**
> ما يبدأ بـ **`web-`** للمتصفّح — وما يبدأ بـ **`app-`** للتطبيق.

---

## المساران منفصلان تماماً

| | المتصفّح | التطبيق |
|---|---|---|
| الرئيسية | `web-home.html` | `app-home.html` |
| دليل المصانع | `web-factories.html` | `app-factories.html` |
| صفحة المصنع | `web-factory.html` | `app-factory.html` |
| المحادثات | `web-messages.html` | `app-messages.html` |
| السلة | `web-cart.html` | `app-cart.html` |
| لوحة الحساب | `web-account.html` | `app-account.html` |
| تسجيل الدخول | `web-login.html` | `app-login.html` |
| حساب جديد | `web-register.html` | `app-register.html` |
| الترحيب | `web-welcome.html` | `app-welcome.html` |
| الإعدادات | `web-settings.html` | `app-settings.html` |
| الملف الشخصي | `web-profile.html` | `app-profile.html` |
| المساعدة | `web-help.html` | `app-help.html` |
| إدارة المصانع | `web-admin.html` | `app-admin.html` |
| شاشة البداية | — | **`index.html`** |
| اختيار نوع الحساب | — | `app-user-type.html` |

⚠️ **`index.html` هي الاستثناء الوحيد بلا بادئة.** تحمل محتوى
التطبيق، لكن الاسم **محجوز في الويب**: الخوادم تعرضه
تلقائياً عند فتح عنوان الموقع. **لا تُعد تسميته.**

---

## الفرق التقني

| | `web-` | `app-` |
|---|---|---|
| `mobile.css` | ✅ | ✅ |
| `desktop.css` | ✅ | ❌ |
| على الكمبيوتر | تصميم عريض | عمود الجوال |
| على الجوال | تصميم الجوال | تصميم الجوال |

ملفان منفصلان تماماً: التعديل في أحدهما **لا يمسّ** الآخر،
وروابط التنقّل داخل كل مسار تبقى فيه.

---

## ملفات الشرح

| الملف | ما فيه |
|---|---|
| `الصفحات.md` | هذا الملف — خريطة الصفحات |
| `CLAUDE.md` | قواعد العمل — يقرأه كلود كل جلسة |
| `قرارات-سابقة.md` | التفاصيل وأسباب القرارات — مرجع عند الحاجة |

فُصِلا لأن ملفاً واحداً كبيراً كان يُقرأ كاملاً في كل جلسة.

---

## أين أعدّل؟

| التعديل | الملف |
|---|---|
| شكل **التطبيق** لكل الصفحات | `mobile.css` |
| شكل **الموقع** لكل الصفحات | `desktop.css` |
| صفحة تطبيق بعينها | ملف `app-...` |
| صفحة موقع بعينها | ملف `web-...` |
| نصّ يظهر للمستخدم | `i18n.js` — للثلاثين لغة |
| قاعدة البيانات | `schema.sql` ← يطبّقه المالك في Supabase |

---

## `auth-guard.js` يختار المسار تلقائياً

الحارس ملف واحد يخدم المسارين، وكانت وجهته ثابتة على نسخ
التطبيق — فزائر الموقع الذي تنتهي جلسته كان يُنقل إلى صفحة
بتصميم الجوال في وسط شاشة عريضة.

**الآن يقرأ اسم الصفحة الحالية ويبقى في مسارها:**

| المستخدم في | يُرسل إلى |
|---|---|
| صفحة `web-` | `web-welcome` / `web-register` |
| غير ذلك | `app-welcome` / `app-register` |

الدوال المسؤولة: `currentPage()` و `isWebPath()` و `loginPage()`
و `registerPage()`. وتُستعمل في ثلاثة مواضع: الطرد عند انتهاء
الجلسة، و`sfRequireLogin()`، وتسجيل الخروج.

**ملاحظة:** `index.html` محتواها تطبيق ولا تحمل بادئة، فتقع
ضمن حالة «غير web-» أي مسار التطبيق — وهو الصحيح.

---

## عند إنشاء نسخة تطبيق جديدة

حذف رابط `desktop.css` وحده **لا يكفي دائماً**. فالقاعدة التي
تُخفي عناصر المتصفّح موجودة داخل `desktop.css` نفسه — فحذف
الرابط يلغي **الإخفاء** أيضاً، فتظهر العناصر بلا تنسيق.

**افحص الملف بحثاً عن:**
`dt-bar` · `dt-catbar` · `dt-actions` · `page-logo` · `dash-side` · `dash-stats`

لا شيء ← حذف الرابط كافٍ. وُجد شيء ← احذف تلك العناصر وسكربتاتها.

---

## الحالة اليوم ٢٠ أغسطس ٢٠٢٦

**جاهز ويعمل:**
- قاعدة البيانات وأمانها — مختبَرة بالهجوم
- التسجيل والدخول على الخادم
- اعتماد المصانع من `web-admin.html`
- فصل كامل بين مساري الموقع والتطبيق

**ناقص:**
- **الواجهات غير موصولة بقاعدة البيانات** — المصانع والمنتجات أرقام مولّدة
- الرسائل محلية: لا تصل لجهاز آخر
- الصور في `localStorage` وسقفها ٥ ميغا
- ربط أزرار الشراء والسلة بتسجيل الدخول

**تنبيهان قبل النشر:**
1. **تأكيد البريد معطّل** — أي أحد يسجّل ببريد لا يملكه
2. **صفحة المصنع تثق بقيمة قابلة للتغيير** — القاعدة فيها العلاج `owner_id`

---

## هذا موقع، وليس تطبيقاً مثبَتاً بعد

يُفتح في المتصفّح فقط. لا يوجد ملف تثبيت ولا وجود في المتاجر.
تحويله إلى تطبيق يُثبّت خطوة منفصلة: PWA مجانية ويوم عمل،
أو المتاجر بأسابيع ورسوم.
