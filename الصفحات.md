# دليل صفحات المشروع

> **المعمارية المعتمدة:** صفحات `web-*` و`index.html` للموقع، ومجلد `app_flutter/` لتطبيق Android وiOS. صفحات `app-*.html` القديمة أزيلت لمنع ازدواجية التطبيق.

## الموقع الإلكتروني

| الوظيفة | الملف |
|---|---|
| الصفحة الرئيسية والمنتجات | `index.html` |
| دليل المصانع | `web-factories.html` |
| صفحة المصنع | `web-factory.html` |
| تفاصيل المنتج | `web-product.html` |
| المحادثات | `web-messages.html` |
| السلة | `web-cart.html` |
| طلباتي — الطلبات المكتملة | `web-orders.html` |
| المفضّلة | `web-favorites.html` |
| الفواتير | `web-invoices.html` |
| تفاصيل الفاتورة | `web-invoice.html` |
| لوحة الحساب | `web-account.html` |
| تسجيل دخول وحساب فرد | `web-login.html` |
| بوابة المصانع | `web-supplier.html` |
| استعادة كلمة المرور | `web-reset.html` |
| الإعدادات | `web-settings.html` |
| الملف الشخصي | `web-profile.html` |
| المساعدة | `web-help.html` |
| إدارة المصانع | `web-admin.html` |

`index.html` هي الصفحة الرئيسية الوحيدة بلا بادئة. الخوادم تعرضها تلقائيًا عند فتح النطاق، لذلك لا يجب تغيير اسمها.

## تطبيق Flutter

التطبيق موجود بالكامل داخل `app_flutter/`. صفحات Flutter تغطي شاشة البداية، اختيار نوع الحساب، التسجيل والدخول، الرئيسية، المصانع، المصنع والمنتجات، الحساب، الملف الشخصي، السلة، الرسائل، الإدارة، الإعدادات، والمساعدة.

عند فتح التطبيق تظهر شاشة ترحيبية لمدة ثلاث ثوانٍ، تضم الشعار ورسماً متحركاً لمصنع وطرود وشاحنة توصيل. بعدها ينتقل الزائر إلى اختيار نوع الحساب، والمسجّل إلى الرئيسية. تحترم الشاشة إعداد تقليل الحركة، ولا يعود إليها زر الرجوع.

| طبقة التطبيق | المسار |
|---|---|
| الصفحات | `app_flutter/lib/pages/` |
| خدمات Supabase | `app_flutter/lib/services/` |
| الإعداد والتهيئة | `app_flutter/lib/core/` |
| نقطة التشغيل | `app_flutter/lib/main.dart` |

### الصفحات والوظائف المنقولة من الويب

| صفحات الويب | المقابل في Flutter |
|---|---|
| `index.html` | `home_page.dart`، صور الفئات والمنتجات، خريطة المناطق وإحصاءات المنصة |
| `web-factories.html` | `factories_page.dart`، البحث وتصفية الفئة والمنطقة |
| `web-factory.html` | `factory_page.dart` و`factory_edit_page.dart`، المتابعة والمنشورات والمنتجات وشرائح الأسعار |
| `web-product.html` | `product_page.dart`، الصور والكميات والتقييمات والمفضلة وبيان المبالغ |
| `web-cart.html` | `cart_page.dart`، كتابة الكمية وتحديث سعر الشريحة وإنشاء الطلب |
| `web-messages.html` | `messages_page.dart` و`chat_page.dart`، بطاقات المنتجات ومرفقات الصور والفيديو |
| `web-orders.html` | `orders_page.dart`، الطلبات وتأكيد استلام الدفع للمصنع أو المدير |
| `web-favorites.html` | `favorites_page.dart` |
| `web-invoices.html` و`web-invoice.html` | `invoices_page.dart` و`invoice_page.dart` |
| `web-account.html` | `account_page.dart`، روابط الصفحات وإيرادات الطلبات المدفوعة |
| `web-profile.html` | `profile_page.dart`، بيانات الحساب؛ عنوان المصنع داخل محرّر المصنع |
| `web-settings.html` | `settings_page.dart`، اللغة وعملة العرض |
| `web-admin.html` | `admin_page.dart`، حالات المصانع وأسباب الرفض وصور الفئات؛ وأضاف التطبيق تبويب إعلانات التخفيضات |

تفاصيل الاختبار والاختلافات المناسبة للجوال في `app_flutter/WEB_PARITY.md`.

## الفصل بين الموقع والتطبيق

| العنصر | الموقع | التطبيق |
|---|---|---|
| الواجهة | HTML/CSS/JavaScript | Flutter/Dart |
| الصفحات | `index.html` و`web-*.html` | `app_flutter/lib/pages/` |
| المصادقة والبيانات | Supabase JS | `supabase_flutter` |
| التصميم | `mobile.css` و`desktop.css` | Widgets وTheme داخل Flutter |
| قاعدة البيانات | مشروع Supabase نفسه | مشروع Supabase نفسه |

الموقع وتطبيق Flutter مستقلان في الواجهة، لكنهما يشتركان في قاعدة Supabase والجداول وسياسات RLS. حذف صفحات `app-*.html` لا يحذف تطبيق Flutter ولا يؤثر في صفحات `web-*`.

## حارس صفحات الموقع

`auth-guard.js` مخصص للموقع فقط. عند انتهاء الجلسة أو محاولة تنفيذ إجراء يتطلب حسابًا، يوجّه المستخدم إلى:

| الحالة | الوجهة |
|---|---|
| تسجيل الدخول | `web-login.html` |
| إنشاء حساب | `web-login.html#register` |

تطبيق Flutter لا يستخدم `auth-guard.js`، بل يدير المصادقة داخل `AuthService`.

## الملفات المشتركة التي يجب عدم حذفها

| الملف | الاستخدام |
|---|---|
| `supabase-config.js` | تهيئة Supabase لصفحات الموقع |
| `auth-guard.js` | حماية تجربة صفحات الموقع وإدارة الجلسة |
| `commerce-service.js` | السلة والطلبات في الموقع |
| `messages-service.js` | الرسائل والمرفقات في الموقع |
| `media-upload.js` | رفع الصور والوسائط في الموقع |
| `messages-badge.js` | عداد الرسائل غير المقروءة |
| `favorites-service.js` | المفضّلة والطلبات والفواتير |
| `i18n.js` و`lang-switch.js` | اللغات والترجمة |
| `mobile.css` و`desktop.css` | تصميم الموقع المتجاوب |
| `schema.sql` و`supabase/migrations/` | مخطط قاعدة البيانات وترحيلاتها |

## التحقق بعد أي تعديل

شغّل من جذر المستودع:

```bash
python3 scripts/validate-static.py
python3 scripts/check_web_integrity.py
```

ثم شغّل الموقع محليًا وتأكد من تحميل `index.html` وجميع ملفات `web-*.html`. ولتطبيق Flutter شغّل:

```bash
cd app_flutter
flutter pub get
flutter analyze
flutter test
```
