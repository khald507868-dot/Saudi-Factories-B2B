# قرارات سابقة وتفاصيل تنفيذية

> ملف **مرجعي**، لا يُقرأ في كل جلسة. يحوي التفاصيل التي
> تُحتاج عند العمل على جزء بعينه: القياسات الدقيقة، وسجلّ
> المحاولات الفاشلة، والأسباب وراء قرارات تبدو اعتباطية.
>
> **`CLAUDE.md` يحوي القواعد؛ وهذا الملف يحوي القصة وراءها.**
> إن ذكرت قاعدة هناك أن التفصيل هنا، فاقرأ القسم المقابل قبل التعديل.

---

## الشعار — القياسات وسجلّ التغيير

- **`index.html` تستخدم أسماء أصناف مختلفة** لنفس الشيء:
  `.wordmark` / `.title` وهي `<h1>` / `.rule` بدل
  `.wm-title` / `.wm-rule`. البحث عن `wm-title` لا يجدها.
  وهي النسخة **المتحرّكة** الوحيدة: `@keyframes titleIn`
  و`ruleIn`، مع `transform-origin: right center` تنقلب إلى
  `left center` تحت `html[dir="ltr"]`.
- **`.w-dark` تتبع الخلفية**: أبيض `#ffffff` على الخلفية
  الخضراء، وأخضر داكن `#04361b` على الصفحات البيضاء.
  و`.w-green` هو `#45a06a` دائماً.
- **`.w-b2b` ذهبي `#c9a227` ومرفوع**، مقاسه بـ`em` ليتبع
  العنوان: `font-size: 0.52em; vertical-align: 0.68em` في
  النسخ الثلاث الكاملة، و`0.62em / 0.58em` في نسخة الترويسة
  الأصغر. ولأنهما نسبيّان، تكبير العنوان لا يحتاج تعديلاً هنا.
- **المقاسات**: 33px للعنوان و260px عرضاً أقصى، تنزل إلى
  26px / 215px عند `@media (max-width: 480px)` في النسخ
  الكاملة. نسخة الترويسة أصغر بكثير — 11px بعرض 108px — لأن
  الشريط العلوي ارتفاعه 58px فقط. وهذا العرض كبر
  72px ← 88px ← 108px مع إضافة النص اللاتيني ثم "B2B"؛
  العربية كانت تكفيها مساحة أقل.
- **الشعار الوصفي `.wm-sub` حُذف من كل الصفحات.** ومفتاحه
  `splash_tagline` باقٍ في الثلاثين كتلة لغة لكنه يتيم.
- **مشهد المصانع الاثني عشر المتحرّك** كان خلف الشعار في
  صفحة الترحيب؛ حُذف كاملاً لأن الشعار فوقه كان غير مقروء.
  ومعه `.factory-scene` و`.factory-illustration` و`.smoke`
  و`@keyframes sceneIn` و`smokeRise`. **لا نسخة باقية منه** —
  آخر نسخة حُذفت بطلب المالك. لا تعرض استعادته ولا تُعِد بناءه.

---

## حركة الشعار — لماذا `clip-path` لا `mask`

الحركة في صفحة الرئيسية فقط، ولا تتوقّف. تنقّح عليها المالك
عبر نحو اثنتي عشرة مراجعة حتى استقرّت على هذا الشكل:

> الكشف **من اليسار** خلال 1.4s ← ثبات كامل 3s ← اختفاء
> فوري ← تكرار من البداية.

`@keyframes wmType` يعمل `4.4s steps(24, end) infinite` على
`.top-bar-logo .wm-title`، و`wmCaret` يحرّك الخط الذهبي على
التوقيت نفسه ليتحرّكا معاً. والثبات هو `inset(0 0 0 0)` ممتدّ
من 31.8% إلى 99.9%، والقفزة من 99.9% إلى 100% تعيد البدء
نظيفاً؛ أما "ترجع من الخلف" فكانت النسخة المرفوضة.

**النص يبقى في DOM** — قصّ لا إعادة كتابة بالجافاسكربت —
ليظل قابلاً للتحديد ولقراءة محرّكات البحث وقارئات الشاشة.
و`prefers-reduced-motion` يوقفها بـ`clip-path: none`.

### لا تُعِدها إلى `mask-image`

جُرِّبت وفشلت مراراً: `mask-size: 200% 100%` مع توقّف حادّ عند
`50%` جعل القناع يقطع نصف عرض العنصر فقط، فلم يصل
`mask-position: 0%` إلى كشفه كاملاً، ووقع الثبات على حالة
**نصف مكشوفة**: "ctories B2B" ظاهر و"Saudi Fa" مخفي. وضاعت
جولتان في اتهام `steps(24, end)` قبل أن تكشف لقطة من المالك
السبب الحقيقي. و`clip-path` نجح لأن `inset(0 0 0 0)` تعني
"ظاهر بالكامل" بلا لبس.

---

## تفاصيل الصفحات

### شاشة البداية

التحويل مكتوب **مرّتين متعمّدتَي الاختلاف**: الجافاسكربت
`1500`ms وهو اختيار المالك، بينما `<meta>` تبقى `2` ثانية،
لأن `content` تأخذ **عدداً صحيحاً** رسمياً — و`1.5` قد تُقتطع
إلى `0` فتحوّل فوراً. الجافاسكربت يفوز في كل متصفّح يشغّله،
والـmeta أرضية للحالة بلا جافاسكربت. **أبقِ الـmeta عدداً
صحيحاً ≥ قيمة الجافاسكربت**، وغيّرهما معاً إن انتقلت الصفحة
الهدف.

والعنوان والخط لكلٍّ منهما **مقاسان**: قاعدة أساسية وتجاوز
عند `@media (max-width: 480px)`. الهواتف تقرأ قيم الـmedia
query، فتعديل القاعدة الأساسية وحدها **لا أثر له على الجهاز
الهدف**. والاقتران نفسه يتكرّر في صفحتَي الترحيب واختيار نوع
الحساب — افحصه قبل ضبط أي مقاس.

### الترحيب

- عند `sf_account_type === "factory"` يتغيّر نص زر التسجيل
  إلى `splash_signup_factory_btn`. والسكربت يضبط **`data-i18n`
  و`textContent` معاً** — الصفة وحدها لا تكفي لأن
  `applyTranslations()` تكون قد عملت قبلها.
- `.auth-btn:active { opacity: 0.85 !important }` تحتاج
  `!important`: حركة الدخول تستخدم `animation-fill-mode: both`
  التي تثبّت `opacity: 1` بعد انتهائها فتبتلع تعتيم الضغط.
  و`.type-card:active` في صفحة نوع الحساب تحمل السبب نفسه.

### التسجيل

- **يحفظ في `sf_account` باستمرار** — عند `input` و`blur`
  و`change` واختيار الدولة — لا عند الإرسال. وهذا متعمّد:
  زرّ الإرسال قد لا يُفعَّل أبداً، ونسخة سابقة كانت تحفظ عند
  الإرسال فقط فضاع كل ما كتبه المستخدم. **أبقِ أي حقل جديد
  على مسار الحفظ المستمر نفسه.**
- كلمة المرور **لا تُحفظ** في `sf_account` إطلاقاً.
- **يتفرّع على `isFactory`**: للمصانع يُحذف حقل الجنس، ويصير
  "الاسم" هو "اسم الشركة"، ويظهر حقل السجل التجاري ولوحة
  العنوان. وللأفراد تُحذف الثلاثة **من DOM كلياً** لا إخفاءً —
  فيصير `crInput = null` وتحرس `checkFilled()` بـ`!crInput ||`.
  وسكربت لوحة العنوان كلّه ملفوف في `if (isFactory)` وإلا كان
  `addressTrigger` سيساوي `null` ويرمي خطأ عند التحميل.
- تحقّق لوحة العنوان: العنوان الوطني المختصر
  `/^[A-Z]{4}[0-9]{4}$/` باتجاه LTR إجباري، والمبنى والفرعي
  `/^[0-9]{4}$/`، والبريدي `/^[0-9]{5}$/`.

### الرئيسية

- `.ksa-map` عرضها `100%` بحدّ أقصى `240px`، **بلا** تجاوز
  عند 480px. صُغِّرت 400 ← 320 ← 240px تحديداً ليظهر أول
  بطاقتَي منتج بلا تمرير — فمقاسها **مقترن بهذا الشرط** لا حرّ.
- **الأكثر مبيعاً**: 1000 بطاقة، اثنتان في الصف، تُولَّد بنص
  HTML واحد يُسنَد لـ`innerHTML` مرّة واحدة. تقرأ `sf_factories`
  وتُخرج سطرَي الاسم والسعر **شرطياً** — لأن `.product-name`
  الفارغة تظل تشغل `max-height: 33px` وهو ما أنتج "المساحة
  البيضاء" التي اشتكى منها المالك، فكان الحل حذف العنصر لا
  ضبط الحشو. وشارات "السنوات" و"معدّل إعادة الطلب" قيم
  تجميلية مشتقّة من الفهرس — `i % 17` و`i % 61` — لتبقى ثابتة
  بين الزيارات بدل أن تتبدّل كل تحميل.
- `.product-name` تحتاج `-webkit-line-clamp: 2` **و**
  `line-clamp: 2` معاً وإلا نبّه المحرّر على الملف.
- **إغلاق نافذة الخريطة يجب أن يمسح الاختيار**: تُزال `.active`
  من كل منطقة **و**يُنفَّذ `blur()` على المسار المركَّز — لأن
  المناطق تحمل `tabindex="0"` فكانت `.region:focus` تُبقي
  التظليل بعد زوال `.active`. سببان منفصلان، وكلاهما لازم.
  أما `selectedRegionName` فلا يُمسح عمداً: زر البحث يقرأه.
- الصفحة **بلا عنوان ولا نصّ تمهيدي**: انتقل العنوان إلى
  شاشة البداية، وحُذف الوصف وشريط "المنطقة المختارة" بطلب
  المالك. والمنطقة المختارة تعيش الآن في المتغيّر
  `selectedRegionName` وحده، **وزر البحث في النافذة يقرأه** —
  وهذا الاقتران هو سبب استحالة حذف الشريط دون إعادة توجيهه،
  وسبب وجوب بقاء المتغيّر في أي تنظيف لاحق يبدو أنه شيفرة ميتة.
- `.map-section` شفّافة بلا خلفية بطاقة. فإن ظهرت لوحة بيضاء
  خلف الخريطة، فالسبب `body { background: #f3f5f3 }` وهو
  **تلوين على مستوى الصفحة لا لوحة** — ولن يزيله أي تعديل
  على `.map-section`.
- **الحدّ الأخضر الخارجي للبلاد مزيّف بالطبقات لا بالهندسة**:
  مجموعة `<g class="ksa-outline">` فيها 13 نسخة
  `<use href="#region-id">` تُرسم **تحت** مسارات المناطق بحدّ
  أخضر عريض، ثم تغطّي تعبئة كل منطقة الحدود الداخلية فلا يبقى
  ظاهراً إلا المحيط الوطني. ويتطلّب هذا بقاء المجموعة **قبل**
  مسارات المناطق في ترتيب المستند، فـSVG بلا z-index. وإضافة
  منطقة تعني إضافة `<use>` لها هنا أيضاً.

### لوحة الحساب

- **مجرّدة عمداً**: على المتصفّح لوحة تحكّم بنمط علي بابا،
  وعلى الهاتف تنكمش إلى صفّ واحد يقود إلى الإعدادات.
  **بلا ترويسة** — حُذفت `.profile-header` الخضراء بطلب
  المالك، بعد صفّ الإجراءات السريعة وصفوف القائمة وأخيراً
  بطاقة المصنع. **أربعة طلبات "احذفها" منفصلة** وصلت لهذه
  الصفحة؛ لا تُعِد إليها أي زينة.
- ولأن الترويسة ذهبت، لا `.page-header` فيها — وهي ما يعلّق
  عليه `mobile.css` هامش الأمان العلوي. فصار الهامش على `body`
  بـ`padding-top: env(safe-area-inset-top)`؛ وحذفه يكسر نتوء
  الآيفون من جديد، وبلا أثر مرئي على سطح المكتب.
- **بطاقة المصنع حُذفت** وسماً وسكربتاً وتنسيقاً. كانت آخر
  واجهة مرتبطة بقاعدة البيانات خارج صفحات المصادقة، فصارت
  الصفحة **بلا أي استدعاء بيانات**. ومعها ذهب الطريق المباشر
  الوحيد إلى صفحة المصنع؛ فالوصول الآن عبر تبويب "المصانع".
- أرقام `.dash-stats` الثلاثة **مكتوبة `0` يدوياً** لا
  استعلامات. والمعرّفات `stat-messages` و`stat-offers`
  و`stat-orders` جاهزة للربط.
- عناوين القائمة الجانبية وروابطها **مربوطة بـ`data-i18n`**،
  وكذلك `.dash-stat-label`. وقد كلّف هذا 15 مفتاحاً جديداً ×
  30 كتلة لغة؛ وأُعيد استخدام 8 مفاتيح موجودة بدل تكرارها.
  وسطر نوع الحساب يُضبط من الجافاسكربت عبر
  `I18N.t("dash_account_factory" / "dash_account_individual")`
  لا بنصّ حرفي — لأن `data-i18n` لا تصله: السكربت يكتب فوق
  `textContent` بعد انتهاء `applyTranslations()`.
- **صفّ "الإعدادات" المكرّر يُخفى على سطح المكتب فقط** عبر
  `#mobile-settings-row { display: none }` في `desktop.css`.
  والمعرّف على `<section>` متعمّد: صفحة الإعدادات فيها **٤**
  أقسام و**١٧** صفاً تتشارك أصناف `.account-section` و
  `.account-row`، فقاعدة مبنية على الصنف كانت ستُفرغ تلك
  الصفحة. ويبقى الصفّ ظاهراً على الهاتف لأن القائمة الجانبية
  خاصة بسطح المكتب، فهو الطريق الوحيد هناك.

### الملف الشخصي

- **الحفظ هنا يخالف صفحة التسجيل عمداً**: يحفظ **فقط** عند
  زر "تحديث"، لا باستمرار. وقد طلب المالك هذا صراحةً. وموضع
  استدعاء `saveAccountData()` واحد فقط هو معالج `submit`؛
  ومستمعات الحقول تستدعي `checkFilled()` وحدها. **لا "تُعِد
  الاتساق"** مع صفحة التسجيل بإرجاع الحفظ عند الكتابة.
- والحفظ الناجح يُظهر `.confirm-overlay` — صندوق مركزي بعلامة
  صح مرسومة بتحريك `stroke-dashoffset` — يختفي بعد 1.8s،
  ونصّه مفتاح `profile_updated`.
- **حقل تاريخ الميلاد**: `<input type="date">` الأصلي طبقة
  **شفّافة تغطّي كامل** الحقل المنسّق بـ`position: absolute;
  inset: 0; opacity: 0`، لذا يجب أن تبقى على `.date-field`
  خاصية `position: relative`. وقد حلّ هذا محلّ نسخة سابقة
  أعطت الحقل `pointer-events: none` وقادته بـ`showPicker()`
  من الغلاف — وكانت غير قابلة للإدخال أصلاً. و`showPicker()`
  باقية محروسة بـtry/catch على نقر الحقل نفسه كتحسين لسطح
  المكتب فقط؛ الطبقة الشفّافة هي ما يعمل فعلاً. والتاريخ
  المختار يُعرض بتفكيك قيمة ISO إلى أجزاء
  `new Date(y, m - 1, d)` لا `new Date(value)` التي تُفسَّر
  UTC فتُظهر اليوم السابق شرق غرينتش. و`dateInput.max` يمنع
  التواريخ المستقبلية.

### دليل المصانع

- **بطاقتان في الصف**، و`row-gap: 24px` أكبر من
  `column-gap: 10px` عمداً: `.logo-placeholder` موضوعة عند
  `bottom: -14px` فتتدلّى خارج البطاقة، ولولا الفجوة الأكبر
  لاصطدمت بالصف التالي. ويجب بقاء `overflow: visible` على
  `.factory-card` لأجل هذا التدلّي — قصّه هو الحلّ الآخر،
  وهو الخطأ.
- `.card-footer` تحجز مساحة الشعار بحشو غير متماثل
  `padding: 0 9px 0 52px`، وله نظير `html[dir="ltr"]` يقلبه.
  **عدّلهما معاً.**

### صفحة المصنع

زرّ "مراسلة المصنع" يُعرض **للجميع** عمداً. نسخة سابقة أخفته
خلف `body.view-only`، فصار المصنع عاجزاً عن المراسلة والأفراد
وحدهم يستطيعونها — وهو عكس المقصود تماماً. **لا تُعِد ربطه
بـ`canEdit`.**

---

## صفحة المنتج — ثلاث محاولات تخطيط مرفوضة

`web-product.html` تحمل `store-page` وحدها بلا صنف خاص،
و**هذا هو الوضع الذي ارتضاه المالك** بعد ثلاث محاولات
لتحسين شكلها على الشاشة العريضة، ختمها بـ"قبل كانت أفضل"
فتراجعت الثلاث دفعة واحدة (`e6f0cb1`).

الفراغ الرأسي بين الصورة والبطاقات ليس علّة تستحقّ إصلاحاً:
فهو أثر مصنع بلا بيانات، ويمتلئ وحده حين يُدخل مصنع
حقيقي منتجاً بصورة وسعر. **ولم يطلب المالك إصلاحه أصلاً.**

والمحاولات الثلاث، لئلاّ تُعاد:

1. **صنف `product-page` بعمودين 420px** — لأنّ شبكة `store-page`
   تضع عناصر `web-factory` بـ`grid-row` صريحة (`#card-name`،
   `#card-about`، `#card-products`) وهي معدومة في صفحة المنتج.
   تشخيص صحيح، ولم يقلّل الفراغ.
2. **نقل القواعد إلى داخل كتلة سطح المكتب** — فقد سقطت بين
   قوس إغلاق `min-width: 1024px` وبداية `max-width: 1023px`،
   أي **خارج الاثنين معاً** فلم تُطبَّق على أي مقاس.
   **وفحص توازن الأقواس كان يمرّ سليماً طوال الوقت** — فالتوازن
   لا يكشف موضع القاعدة.
3. **`grid-row: 1 / -1` للصورة** لأنّ حجز الصفّ الأوّل وحده يمدّده
   إلى ارتفاع الصورة المربّعة. جعل الأزرار تهبط أبعد.

والدرس أنّ الثلاث بُنيَت على قراءة الشيفرة لا على قياس،
وكلّ واحدة أصلحت شيئاً حقيقياً لكنّه ليس **ما يرسم البكسلات**.
فإن طُلب تغيير التخطيط لاحقاً، **اطلب من المالك فتح F12
وقياس العنصر** بدل الاستنتاج — فأنت لا ترى الصفحة.

---

## المحادثات — التفاصيل

- صفحة واحدة بمنظورين تبدّلهما `body.in-chat`: قائمة المحادثات
  والمحادثة نفسها. ولا توجيه فيها — `openChat(id)` و
  `closeChat()` تقلبان الصنف فقط.
- **الدور من `sf_account_type`**:
  `myRole = accountType === "factory" ? "factory" : "individual"`.
  والفقاعات `.mine` خضراء أو `.theirs` بيضاء. ولأن متصفّحاً
  واحداً يحمل الدورين، فتبديل نوع الحساب **يُعيد تلوين السجل
  القائم** — وهذا متوقّع لا خلل.
- **مدخلان لازمان**: `?factory=<id>` من زر المصنع، وزرّ "+"
  يفتح لوحة اختيار مصنع مكرّرة في الحالة الفارغة. فالرابط
  العميق وحده كان التصميم الأصلي، وكان يترك القادم من الشريط
  السفلي بلا محادثات عالقاً.
- **المرفقات**: الصور تُصغَّر إلى 640px عبر `resizeImage`.
  والفيديو **لا يُصغَّر** إذ لا مسار canvas له، ويُرفض فوق
  **2MB** — وحدّ `localStorage` قرابة 5MB هو السبب كلّه.
  و`pushMessage` تتراجع بـ`t.messages.pop()` إذا رمت
  `saveThreads()`، فيضيع المرفق بدل أن تفسد المحادثة.
- **أربعة مدخلات ملفات لا اثنان**: `capture="environment"` هو
  ما يفتح الكاميرا مباشرة على الهاتف، لكنه **يمنع** اختيار ملف
  موجود — فلكل نوع وسائط مدخل معرض ومدخل كاميرا، يُختاران من
  `.source-sheet`:
  ```html
  <input type="file" id="image-input"   accept="image/*">
  <input type="file" id="image-capture" accept="image/*" capture="environment">
  ```
  و`capture` مُتجاهَلة على متصفّحات سطح المكتب، فكلا المسارين
  يفتح حوار ملفات هناك — **لا يمكن التحقّق من هذا إلا على هاتف
  حقيقي.**
- `.composer` بـ`position: fixed` فيجب أن تكون في قائمة تحديد
  عرض عمود الهاتف في `mobile.css` وإلا امتدّت عبر النافذة على
  سطح المكتب. سقطت سهواً أول مرة وكشفها المالك.

---

## الطبقة العريضة — تفاصيل التنفيذ

- **إلغاء تحديد عرض `body` لا يكفي وحده — افحص حاوية الصفحة
  نفسها.** `.content-wrap` في صفحة المصنع تحمل
  `max-width: 520px` داخلياً، وهي القيد الحقيقي لا `body`:
  فتوسيع `body` من 1180px إلى 1500px لم يغيّر شيئاً مرئياً
  ما دامت قائمة. **وضاعت ثلاث جولات** في إعادة توسيع `body`
  قبل أن يكشف البحث عن `max-width` في `<style>` الصفحة السبب.
  وقاعدة `store-page` تضبط الآن
  `max-width: none !important; width: 100% !important`.
  **ابحث عن `max-width` في الصفحة قبل افتراض أن القيد في
  `mobile.css`.**
- هوامش الهاتف `margin: 0 14px` على `.card` و`.contact-btn`
  تصارع أعمدة الشبكة أيضاً، فتُصفَّر لكل نمط.
- **حدّد مواضع الشبكة بمعرّفات صريحة لا `nth-of-type`.** فهي
  تعدّ بالوسم، و`.cover-block` و`.storage-warning` عناصر
  `<div>` أيضاً — فينزاح ترقيم البطاقات وتهبط كلّها في العمود
  الخطأ. والحلّ `#card-name` و`#card-about` و`#card-products`.
- **`1fr` لا تصغر تحت محتواها.** فشبكة منتجات داخل عمود `1fr`
  تدفعه ليتّسع فتشوّه كل ما فيه؛ استخدم `minmax(0, 1fr)`.
- **ضع العمود الضيّق أولاً في `grid-template-columns`.** فأرقام
  الأعمدة منطقية وتنقلب مع RTL، وتخطيط "الضيّق ثانياً" يهبط
  في الجهة الخطأ بالعربية. وجعله أولاً يوقعه يميناً في RTL
  ويساراً في LTR تلقائياً، بلا نظير `html[dir="ltr"]`.
- **`padding-top: 0 !important` على `body` حمّالة.** فالصفحات
  تحجز مساحة علوية للشريط **الثابت** في الهاتف؛ وعلى سطح
  المكتب يصير الشريط في التدفّق العادي فيتحوّل الحجز إلى فجوة
  فارغة تدفع المحتوى للأسفل. وقد اشتكى المالك من هذا بالضبط:
  "الموقع من فوق نازل". فإن عادت فجوة أعلى صفحة، فهذا السبب
  لا هامش على أول عنصر.

### لوحة الفئات — أي طرف تلتصق به

- **زرّ "الفئات" في *بداية* اتجاه القراءة، واللوحة تلتصق بطرف
  الزر نفسه.** فـ`.dt-catbar-inner` هي `row-reverse` و`row`
  في LTR، والزرّ **أول أبنائها** — فتقرّر `order` موضعه،
  والإشارة معكوسة الحدس: تحت `row-reverse` تجلس **أدنى** قيمة
  `order` في الطرف **الأيسر** بصرياً. لذا يأخذ الزرّ `order: 1`
  في RTL فيقع أقصى اليمين، و`order: -1` في LTR فيقع أقصى
  اليسار. **لا تقلب `flex-direction` للحاوية** لتحريكه، لأن
  روابط الفئات الثماني تُضاف بالجافاسكربت بالترتيب عبر
  `cats.slice(0, 8).forEach` فيُعكَس ترتيبها المرئي أيضاً.
- واللوحة بعرض 780px تُثبَّت على الطرف نفسه وتنمو **إلى
  الداخل**: `right: 0` في RTL، و`right: auto; left: 0` في LTR.
  **و`right: auto` حمّالة** — بدونها يبقى الطرفان فاعلين فتُشدّ
  اللوحة بينهما بدل الحفاظ على عرضها.
- **عمودا اللوحة لا ينعكسان.** فـ`.dt-catlist` أي الأسماء
  **يسار**، و`.dt-catpreview` أي الدوائر **يمين** في كل
  اللغات — اختار المالك هذا الترتيب بصرياً، فهو **قرار تصميم
  ثابت** لا نتيجة اتجاه قراءة. لذا تُغفل قاعدة `html[dir="ltr"]`
  خاصية `flex-direction` **كلياً** فترث `row` الأساسية، وتقلب
  `right` و`left` فقط وهما ما يجب أن يتبع الزرّ فعلاً.
  - وأمران يتبعان العمود صامتَين وانكسرا في الإنجليزية فقط:
    الفاصل كان `border-inline-end` وهي قيمة **منطقية** تنقلب
    مع `dir`، فهبط الخط على الطرف الخارجي — وصار الآن
    `border-right` الفيزيائية. وسهم `.dt-catlist li a::after`
    كان له نظير يبدّل `‹` و`›`؛ ومع ثبات المعاينة يميناً صار
    `‹` العربي يشير بعيداً عنها، فوُحِّد السهم `›` في
    الاتجاهين وحُذف النظير.
- **نسخة أقدم من التوثيق سجّلت العكس**: أن `left: 0` صحيحة في
  الاتجاهين، وأن عكس الالتصاق هو الخطأ. وكان ذلك صحيحاً **فقط**
  حين كان الزرّ في *نهاية* اتجاه القراءة؛ ونقله إلى البداية
  قلَب أي طرف يقصّ. فإن فاضت اللوحة عن الشاشة ثانيةً، فافحص
  في أي طرف يقع الزرّ قبل تغيير الالتصاق — يجب أن يتوافقا دائماً.

---

## زوايا الإطار مربّعة — أربع محاولات فاشلة

`body::after` يرسم إطاراً داخل كل صفحة. **وزواياه مربّعة
بـ`border-radius: 0` عمداً**، لأن الأشرطة الثابتة التي يلاقيها
— `.top-bar` و`.page-header` و`.bottom-nav` و`.checkout-bar` —
كلّها مربّعة الزوايا، فأي تدوير يظهر كانحناء لا يحاذيها.

وُصِل إلى هذا بعد أربع محاولات مرفوضة، الثلاث الأولى على
الرئيسية التي شريطها العلوي `#04361b` بلون الإطار نفسه فابتلع
حافّته العليا:

1. `border-top-color` أبيض — قُرئ كخطّ أبيض منفصل.
2. تدوير زوايا الترويسة نفسها إلى `15px` أي 18 − 3 لتصير
   متحدة المركز مع الإطار.
3. تربيع الزاويتين العلويتين للإطار وحدهما — فبقي الزوجان
   السفليان منحنيين أمام الشريط السفلي.
4. تربيعها عبر صنف `body.dark-header` — أصلح الصفحتين
   الحاملتين للصنف فقط وترك الباقيتين منحنيتين.

**وما نجح: تربيع قاعدة `body::after` المشتركة نفسها لكل
الصفحات دفعة واحدة**، ثم حُذف `dark-header`. فلا تُعِد تدويراً
هنا، ولا تُعِد المحاولة صفحةً صفحة — فالصنف الخاص بالصفحة هو
بالضبط النهج الذي فشل.

---

## درج الفئات — تحويل `50vw` وسبب بقائه

في `@media (min-width: 520px)` يُعاد وضع `.categories-drawer`
عند حافّة العمود ويُمدّد تحويله **المغلق** بـ`50vw` لأن حافّة
الشاشة لم تعد تخفيه بعد التوسيط.

**وهذا متعمّد وقد نجا من تراجع واحد بالفعل.** فقد غُيِّر إلى
نظام مثبَّت يميناً في الاتجاهين لإصلاح "الدرج يظهر من خارج
الإطار"، فطلب المالك إعادته: "اعدها مثل قبل". **فاترك
`translateX(calc(100% + 50vw))` ونظيره وشأنهما** ما لم يُطلب
ثانيةً — ولاحظ أن الشكوى كانت عن *أين تبدأ الحركة*، لا عن
الموضع النهائي.

---

## الخريطة التفاعلية — أرقام مضبوطة لا تُرخى

`regions-geo.js` يحمل `SA_REGION_BOUNDS`: 13 منطقة كحلقات
`[[[lat, lon], ...]]` **بترتيب Leaflet لا GeoJSON**. والمصدر
OpenStreetMap عبر Nominatim **برخصة ODbL**، مبسّطة بترشيح
المسافة العمودية عند تسامح **`0.0015`** ومحدودة بأكبر **8**
حلقات لكل منطقة، فيبلغ حجمه نحو 234KB. وهو **ملف ساكن لا جلب
وقت التشغيل** — إذ كان Overpass ينتهي بمهلة متكرّرة، والتطبيق
يجب أن يعمل بلا اتصال عند التحميل.

**وهذه الأرقام ضُبطت لإصلاح شكوى حقيقية** هي "الحدود غير
متطابقة"، فلا تُرخَ اعتباطاً. فالقيم السابقة — `0.012` وثلاث
حلقات و48KB — تركت **11.8 كم** بين نقطتَي حدود متجاورتين،
فكانت القطع المستقيمة تقطع الساحل بوضوح عند مستويات التكبير
التي تُفتح عليها المناطق الكبيرة. و`0.0015` تنزل بها إلى
**3.2 كم**. وحدّ الحلقات الثلاث كان يُسقط جزر البحر الأحمر
والخليج للمناطق الساحلية وفيها 119 إلى 256 حلقة لكلٍّ،
ورفعه إلى 8 كلّف نحو 6KB فقط.

### إعادة التوليد — فخّان وقعنا فيهما

- **البحث الحرّ يطابق السجل الخطأ صامتاً.** فـ`ryiadh` و`hail`
  أعادا مكتب اتصالات بـ`addresstype: office` وحجم 630 بايت.
  استخدم المعاملات **البنيوية** مثل
  `state=Ha'il&country=Saudi Arabia` بدل مصارعة الاستعلام
  الحرّ — فقد فشلت أربع محاولات لإعادة صياغته.
- **لا تحلّل JSON بـawk.** فمسح 1.5MB حرفاً حرفاً يستغرق أكثر
  من ثلاث دقائق وينتهي بمهلة. استخرج أولاً أسطر `"lon lat"`
  بـ`sed` و`tr` في 0.8s، ثم أعطِ awk مدخلاً رقمياً نظيفاً.
  وهو الدرس نفسه من عمل i18n: **أعطِ awk مدخلاً صغيراً
  مُهيكلاً.**

وسياسة Nominatim تشترط طلباً واحداً في الثانية على الأكثر
ووكيل مستخدم معرِّف — فسكربت البناء ينام 1.2s بين
الاستعلامات الثلاثة عشر.

### تفاصيل الرسم

- تُبنى الخريطة من حساب Web Mercator عبر `lonToX` و`latToY`
  فوق بلاطات OSM الخام. و`TILE = 256` و`MIN_Z = 5` و
  `MAX_Z = 12` — و12 تكفي للشوارع الرئيسية وتُبقي عدد
  البلاطات مؤدَّباً. و`fitZoom()` تختار أكبر تكبير يظل صندوقه
  داخل الإطار.
- **والعزل بقناع CSS لا بالقصّ**: `renderMap()` تكتب SVG
  مضمّناً كـ`data:` URI في `--region-mask`، وتطبّقه
  `.tile-layer` عبر `mask-image`. وخطّ الحدود المرئي طبقة
  `.region-outline` منفصلة فوقه. وكلاهما من `ringsToPath()`
  فيتوافقان دائماً.
- والتحريك بسحب المؤشّر؛ ويحتاج الغلاف `touch-action: none`
  وإلا مرّر المتصفّح الصفحة بدلاً منه.
- **لا تحاول "أرض بيضاء وشوارع خضراء" بمرشّحات CSS.** فقد
  طُلبت وجُرّبت وثبتت استحالتها: شوارع OSM النقطية **أفتح**
  من الأرض حولها، فأي مرشّح عام يبيّض الأرض يبيّض الشوارع،
  والعكس يجعل الأرض سوداء. وقد تُحقّق بمحاكاة سلسلة
  sepia و hue-rotate و saturate و brightness. وتحقيقه يحتاج
  مزوّد بلاطات متجهة بنمط مخصّص مثل MapTiler أو Mapbox، أي
  مفتاح API واعتماد جديد. **فالتلوين الحالي هو النتيجة
  المقبولة.**
- واستخدام بلاطات OSM ضمن السياسة لمشروع بهذا الحجم، ورابط
  الإسناد `.map-attrib` **إلزامي** — لا تحذفه.

---

## i18n — تفاصيل التغطية والإضافة

### طبقات التغطية — مقايضة معلنة لا سهو

- `dict` فيه ترجمة كاملة لـ**30 لغة**، لكلٍّ **191 مفتاحاً**.
  والملف كلّه تقريباً هذا القاموس؛ والمنطق الفعلي آخر مئة سطر.
- **منتقي اللغات مصفوفة ثلاثيات** من الاسم العربي والاسم
  الأصلي والرمز، معلنة داخل صفحتَي الإعدادات باسم
  `var languages` وفيها **90 مدخلاً**. وقد انتقل مع بقية صفوف
  الحساب، فهو **ليس** في صفحة الحساب رغم الوصول إليه منها.
  وليس مُصدَّراً من `i18n.js` أيضاً — فإضافة لغة تعني تعديل
  ملفّين. والستّون خارج طبقة الثلاثين تُبدّل فعلياً وتسقط إلى
  الإنجليزية عبر سلسلة الاحتياط، لا أن تفعل لا شيء.
- و`regionNames` بمناطقها الثلاث عشرة و`categories` بفئاتها
  العشرين مترجمة **عربي وإنجليزي فقط** بصرف النظر عن طبقة
  الثلاثين — لأن أسماء الأعلام والمفردات التقنية قُدِّر أن خطر
  ترجمتها الخاطئة أعلى من زينة الواجهة. **فلا توسّع هذا
  صامتاً.**

### تقنية إضافة مفتاح إلى الثلاثين

ثلاثون تعديلاً يدوياً بطيء وسهل الخطأ. **وما ينجح**: اكتب
الترجمات الثلاثين في ملف **بترتيب لغات `dict`**، ثم مرّر
`awk` على `i18n.js` مثبّتاً على مفتاح موجود يظهر مرّة واحدة
في كل كتلة مثل `/^      btn_update: /`، واطبع السطر الجديد
بعد كل تطابق. واجعل كتلة `END` تقارن عدد الإدراجات بعدد
الترجمات و`exit 1` عند الاختلاف — **فبدون هذا الحارس** تكتب
مرساة منزاحة ملفاً مزاحاً مشوّهاً صامتاً. واكتب إلى ملف مؤقّت
وانسخ فوق الأصل عند النجاح فقط.

### مفاتيح يتيمة — لا تنظّفها

`splash_tagline` و`usertype_title` و`usertype_subtitle`
و`quick_*` الأربعة و`account_greeting_label` و
`account_guest_name` موجودة في الثلاثين كتلة ولا يشير إليها
أي HTML — إذ حُذفت عناصرها بطلب المالك. **وتُركت عمداً**، لأن
حذف مفتاح من 30 كتلة تعديل جماعي محفوف بالخطر بلا مكسب. فلا
"تنظّفها" بلا طلب، ولا تعتبر وجودها دليلاً على بقاء العناصر.

---

## قاعدة البيانات — تفاصيل الأمان

### الثغرتان اللتان وُجدتا بالهجوم لا بالمراجعة

`with check (id = auth.uid())` تتحقّق **ممّن** يعدّل، **لا ممّا**
يعدّله. وكلتا الثغرتين كانتا حيّتين في نسخة سابقة من المخطّط،
ووُجدتا بمهاجمة الواجهة الحيّة فعلياً بـ`PATCH` عبر curl:

- مصنع يرسل `{"status":"approved"}` على **صفّه هو** ←
  اعتماد ذاتي ناجح، يجعل مسار المراجعة كلّه ديكوراً.
- وأي مستخدم يرسل `{"is_admin":true}` على **ملفه هو** ←
  صلاحية إدارة كاملة، بما فيها قراءة كل الملفات.

والإصلاح في القسم **8.5**: حرّاس `BEFORE UPDATE` هما
`guard_profile_columns` و`guard_factory_columns` يعيدان
الأعمدة المحميّة إلى `old.<col>` صامتَين ما لم يكن
`public.is_admin()`. والمحميّة: `is_admin` و`account_type`
و`id` على الملفات؛ و`status` و`rejection_reason` و`owner_id`
على المصانع. و**يُعيدان ولا يرميان خطأ**، فتنجح الكتابة
الخبيثة بلا أثر.

**وأي عمود جديد يجب ألّا يضبطه المستخدم لنفسه يحتاج سطراً في
الحارس المناسب — فالسياسة وحدها لا تكفي.** وأعد الاختبار
بالهجوم: أرسل `PATCH` فعلياً وتأكّد أن القيمة لم تتغيّر.

### أسماء المشغّلات ليست أسماء الدوال

الدوال `guard_profile_columns` و`guard_factory_columns`،
لكن المشغّلات التي تستدعيها **`profiles_guard`** و
**`factories_guard`**. وكتابة `guard_profile_columns_trg` في
`alter table ... disable trigger` أنتجت
`ERROR: 42704 trigger does not exist`. **فابحث عن
`create trigger` في `schema.sql` قبل تسمية أحدها.**

### منح أول صلاحية إدارة

يتطلّب **تعطيل `profiles_guard`**، لأن الحارس يعيد `is_admin`
لكل من ليس مديراً أصلاً — بما في ذلك التعديلات من Table
Editor، وهذا سبب ارتداد المربّع إلى `false` صامتاً. والوصفة
العاملة: معاملة واحدة تعطّل المشغّل وتحدّث الصفّ وتعيد تفعيله
**في اللصقة نفسها**. **ولا تترك المشغّل معطّلاً أبداً.**

ودوال المساعدة `is_admin()` و`owns_factory(fid)` و
`in_conversation(cid)` هي
`security definer ... set search_path = public` — لأنها
تُستدعى من داخل سياسات الجداول التي تستعلمها، فبدون
`security definer` تتكرّر RLS لا نهائياً.

---

## ملاحظات تاريخية متفرّقة

- **`perl -0pi -e` عطّل نفسه صامتاً** على هذه الملفات عند
  إدراج وسوم `<script>`؛ و`sed -i '<line>a ...'` نجح.
  **فابحث في الملف بعدها دائماً** للتأكّد أن الإدراج وقع.
- **`grep -c` الذي لا يجد شيئاً يخرج بحالة غير صفرية**
  فيُجهض سلسلة `&&` مبكراً، فتبدو الفحوص اللاحقة كأنها لم
  تعمل. فأضف `|| true` عند عدّ ما قد يغيب مشروعاً.
- **التحقّق من وجود مستخدم عبر عدّ REST مجهول لا يثبت شيئاً**
  — فـRLS تخفي المصانع غير المعتمدة وملفات الآخرين عن المفتاح
  المجهول، فالنتيجة الفارغة هي **حالة النجاح المتوقّعة**.
  افحص Supabase ← Authentication ← Users، أو اطلب لقطة.
- **`Start-Process` يفشل على عناوين `file:///` المرمّزة**
  بنسبة مئوية، لا على سلاسل الاستعلام وحدها. فمرّر المسار
  الويندوزي الصريح.
- `fix-privileges.sql` و`cleanup-test-data.sql` سكربتان
  لمرّة واحدة نُفِّذا بالفعل، ومحتواهما مدموج في `schema.sql`
  في القسم 8.5؛ ويُحتفظ بهما كسجلّ.
