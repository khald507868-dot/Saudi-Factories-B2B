# مراجعة ضغط قاعدة البيانات — 12 سبتمبر 2026

## الأدلة

- أظهر رسم البنية التحتية معالجاً قرب 69% وذاكرة قرب 59% ومساحة قرص 19% على Nano.
- أظهرت صادرات `query_load_snapshot.sql` لنمط تهيئة الطلبات بصلاحية authenticated زيادات بمعدل 1207 ثم1200 ثم1147 تنفيذًا في الثانية. لم تتغير لحظة تصفير الإحصاءات بين القراءات.
- آخر مقارنة، بين `19:42:48.128786+00` و`19:45:09.803504+00`، سجلت 162553 تنفيذاً في141.674718 ثانية، بزيادة2633.891 مللي ثانية في وقت التنفيذ. عدد التنفيذات ليس عدد المستخدمين، ووقت هذا الاستعلام وحده لا يفسر استهلاك المعالج الكلي.
- أظهرت لقطة الاتصالات `19:47:06.850123+00` اتصالين لـPostgREST14.5. لم تُظهر صفوف replication slots لقاعدة البيانات وقت الفحص. اللقطة وحدها لا تثبت مصدر الضغط ولا تنفي نشاطاً سابقاً.
- أظهر سجل Postgres اللاحق عشرات آلاف الأخطاء المتكررة: `40001: Factory data changed in another session; reload before saving`، بينما سجل API المعروض لم يُظهر تدفقاً مشابهاً من الطلبات المكتملة.

## السبب والإصلاح

تستعمل دالة المشروع `save_factory_content` الرمز40001 لتعارض نسخة المحرّر مع `updated_at`. توثّق Supabase أن PostgREST14 يعيد معاملات هذا الرمز تلقائياً وقد يدخل في حلقة لا تنتهي، تستمر بعد إغلاق العميل. هذا يطابق نسخة الخادم ورسالة الخطأ في المشروع. لم يُشغّل اختبار إغراق على الإنتاج.

الترحيل `supabase/migrations/20260912200000_stop_factory_conflict_retries.sql` يستبدل رمز التعارض وحده بـPT409 في تعريف الدالة الموجود. يفحص التعريف المتوقع قبل التغيير، ويحتفظ بهوية الدالة وصلاحياتها وباقي جسمها، بما فيه فحص الملكية والطابع الزمني ومعاملة الحفظ. يدعم إعادة التشغيل. حُدث تعريف schema.sql أيضاً؛ بقيت الترحيلات التاريخية كما هي.

تعرض Flutter رسالة التعارض المترجمة للرمزين القديم والجديد. يوقف محرّر الويب الحفظ التلقائي المؤجل عند التعارض، ويحتفظ بالمسودة المحلية وتحذير مغادرة الصفحة. يجب إعادة فتح المصنع ومراجعة النسخة الحالية قبل محاولة دمج التعديلات؛ لا يُلغى شرط النسخة ولا يُعاد الحفظ بطابع أحدث بصورة عمياء.

اجتاز `scripts/test_factory_conflict.mjs` تسعة اختبارات معزولة: إعادة إنتاج الرمز القديم، حفظ جسم الدالة وصلاحياتها وتكرار الترحيل، رفض الكتابة القديمة، حماية الملكية، نجاح حفظ المصنع والمنتج والمنشور بالنسخة الصحيحة، رفض تعريف غير متوقع، إيقاف طابور الويب بعد التعارض مع حفظ المسودة، تمرير النسخة الجديدة إلى الحفظ التالي، وإمكانية محاولة لاحقة بعد خطأ شبكة. اختُبر باستخدام تعريف الإنتاج المصدّر؛ لا يحاكي خادم PostgREST الفعلي أو يثبت انخفاض CPU على الإنتاج.

التشغيل: `node scripts/test_factory_conflict.mjs app_flutter/build/video-ad-validation [diagnostic-json]` بعد توفر حزمة PGlite في مجلد الاختبارات المحلي.

## التطبيق والتحقق المتبقيان

الإصلاح جاهز ولم يؤكد المالك تطبيقه على قاعدة الإنتاج بعد. ملفات التشخيص والترحيل محفوظة مع كود المشروع.

1. تطبيق الترحيل في SQL Editor.
2. الحصول على `process_id` من تفاصيل خطأ40001 حديث ومطابقته باتصال PostgREST الحالي قبل إيقاف الاتصال العالق وحده. وفق Supabase، تعديل الدالة لا يكفي لإيقاف المعاملات التي بدأت سابقاً. لا تُنهى جميع الاتصالات ولا يُعاد تشغيل المشروع تلقائياً.
3. تحديث صفحة محرّر الويب لتشغيل الكود الجديد، وإعادة بناء نسخة Flutter عند الحاجة لتوزيع رسالة الخطأ الجديدة؛ إصلاح قاعدة البيانات لا يعتمد على إعادة بناء العميل.
4. التحقق من توقف السجلات المتكررة وانخفاض معدل الاستدعاءات ومراجعة رسم CPU الحديث، ثم تجربة حفظ صحيح ورفض حفظ متعارض. بيانات الرسم التاريخية لن تختفي عند الإصلاح.

المصدر: [Supabase: SQLSTATE40001 في RPC يسبب إعادة محاولات لا تنتهي](https://supabase.com/docs/guides/troubleshooting/high-cpu-and-infinite-transaction-retries-when-using-custom-error-codes-in-rpc-functions-77326b).
