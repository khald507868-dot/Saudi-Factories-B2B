# مراجعة أمان — 17 سبتمبر 2026

## النتيجة والنطاق

كشفت مراجعة النسخة المحلية خللين في إنشاء الطلبات وثلاثة مواضع لإدراج صورة الحساب كنص HTML. أُصلحت في الملفات واختُبرت محلياً. **أكد المالك نجاح تشغيل ملف إصلاح الطلبات برسالة «Success. No rows returned»، ثم أرسل صورة فحص إجماليات الطلبات وكانت النتيجة صفراً. التصدير اللاحق لتعريفات الإنتاج يؤكد مطابقة جسم دالة إنشاء الطلب للإصلاح ومنع الزائر من استدعائها. كشف التصدير أيضاً مشكلتي خصوصية وخطأ في مسار رفع الشهادات؛ أُعد لها إصلاح جديد وأرسل المالك صورة نجاح تنفيذه ثم تصدير التحقق الذي اجتازت فيه الإعدادات الستة الفحص. لا تعني هذه المراجعة خلو المشروع أو الإنتاج من جميع الثغرات.**

شملت المراجعة مسارات المصادقة والصلاحيات والطلبات والرسائل والعروض الخاصة والعناوين والشهادات والتخزين وحذف الحساب، وفحص أنماط الأسرار واعتماديات Flutter. الاختبارات تستخدم بيانات وهمية في PostgreSQL/PGlite بالذاكرة؛ لا تقرأ بيانات العملاء ولا تحذف ملفات المستخدم. لم يُشغَّل بناء Flutter أو تنظيف مجلداته.

## النتائج المؤكدة والإصلاح

| النتيجة | الأثر | الحالة |
|---|---|---|
| عالية: إعادة استخدام مفتاح طلب سابق تُضيف أصنافاً إليه | دالة `create_order_from_cart` في ترحيل الأسعار تستخدم `ON CONFLICT ... DO UPDATE` ثم تُدرج الأصناف دائماً؛ أُعيد إنتاج إضافة أصناف لطلب مدفوع مع بقاء حالته وإجماليه القديمين | ترحيل جديد يعيد الطلب السابق دون أي كتابة أو استهلاك السلة الجديدة، ويرفض استخدام مفتاحه لمصنع مختلف |
| متوسطة: تجاوز اعتماد مصنع عند إنشاء طلب | ترحيل الأسعار أزال فحص اعتماد المصنع؛ أُعيد إنتاج طلب منتج لمصنع مرفوض لا يستطيع المشتري قراءة منتجه مباشرة | الترحيل الجديد يفحص الاعتماد في الخادم ويقفل صف المصنع أثناء إنشاء الطلب |
| تحصين ضد حقن HTML في صورة الحساب | `company_image` تدخل مباشرة في `innerHTML` بصفحات الطلبات والفواتير والمفضلة؛ قيمة تحتوي علامات اقتباس يمكنها كسر سياق الخاصية. المسار المرصود يعرض صورة الحساب نفسه؛ لم يثبت مسار لاستغلال حساب آخر | إنشاء عنصر `img` عبر DOM مع قبول روابط HTTP/HTTPS فقط، دون تفسير القيمة كـ HTML |

الإصلاح: `supabase/migrations/20260917100000_checkout_security.sql`. يحافظ على توقيع الدالة، وتسعير الشرائح والسعر الخاص، ورسوم التوصيل 30 والرسوم 1% والضريبة 15% والدفع اليدوي. يلتقط أسعار وكميات الأصناف مرة واحدة، ويرفض الأسعار غير الصالحة وNaN وتجاوز مجال الإجمالي، ويحسب المجموع من مبالغ السطور المقربة إلى هللتين. هذا يمنع اختلاف المجموع عن مجموع السطور في الكميات الكسرية. لا يحذف من السلة إضافات لم تدخل في لقطة الطلب.

الترحيل قابل لإعادة التشغيل ولا يعيد كتابة الطلبات القديمة. **إن كانت هناك طلبات متأثرة سابقاً، فإن منع الثغرة لا يصلحها تلقائياً.** استعلام التشخيص أدناه يعرض عدد حالات عدم تطابق مجموع الأصناف مع subtotal؛ الاختلاف يستدعي مراجعة ولا يثبت الاستغلال وحده.

## التحقق

- `node scripts/test_checkout_security.mjs app_flutter/build/video-ad-validation`: تسع مجموعات، تشمل إعادة إنتاج الخللين على التعريف السابق، منعهما بعد الإصلاح، تكرار الترحيل، سلامة الطلب المدفوع وإعادة المحاولة، عزل المشتري، تأكيد البريد، التسعير، المدخلات غير الصالحة ولقطة الأصناف. اختبار تغير البيانات محاكاة حتمية بين مراحل المعاملة، وليس اختبار ضغط لتزامن اتصالات إنتاج متعددة.
- `node scripts/test_avatar_security.mjs`: 15 حالة للقيم السليمة وعلامات الاقتباس وبروتوكولات JavaScript/data/file، بتشغيل مقاطع العرض الفعلية مع DOM اختباري. لم يُجر اختبار بصري بمتصفح.
- اجتازت 11 مجموعة الاختبارات السابقة: `account_access`, `account_approval`, `account_deletion`, `delivery_addresses`, `factory_resubmission`, `factory_certificates`, `private_offers`, `message_product_security`, `internal_function_permissions`, `tier_price_permissions`, `storage_listing`.
- اجتاز اختبار بيانات بطاقات المنتجات، وفحص 654 مرجع ترجمة Flutter، وتحليل صياغة 25 سكربت HTML داخلي في الذاكرة.
- فحص أنماط مفاتيح Supabase السرية وJWT بصلاحية service_role والمفاتيح الخاصة وGitHub/AWS في ملفات المصدر المتتبعة لم يجد مطابقات عالية الخطورة. هذا فحص أنماط للنسخة الحالية وليس فحصاً كاملاً لتاريخ Git. المفتاح المنشور في العميل `sb_publishable_` عام بطبيعته.
- فحص OSV عبر API لعدد **143** مكتبة Pub مثبتة في `app_flutter/pubspec.lock`: **صفر مطابقات** لثغرات منشورة وقت الفحص. لا يشمل هذا محرك Flutter أو مكونات Android أو حزم JavaScript غير المثبتة بإصدار محدد، ولا يضمن غياب ثغرات غير منشورة.
- أُضيف تشغيل اختبارات الأمان إلى GitHub Actions بقاعدة اختبار مستقلة وإصدار PGlite ثابت. تشغيل CI المستضاف نفسه لم يحدث في هذه الجلسة.

## تطبيق إصلاح الطلبات

**الحالة:** أكد المالك نجاح التنفيذ بعد توجيهه إلى ملف الترحيل الجديد. وأظهرت الصورة اللاحقة `orders_with_item_subtotal_mismatch = 0`؛ أي لا يوجد اختلاف بين subtotal ومجموع مبالغ الأصناف وقت الفحص. وطابق تصدير التعريفات اللاحق جسم دالة إنشاء الطلب مع الإصلاح، مع `anon_execute=false` و`authenticated_execute=true`. لا يثبت هذا خلو الطلبات من كل أنواع التلاعب؛ تجربة طلب جديد ونشر صفحات الويب ما زالا غير مؤكدين.

1. في Supabase SQL Editor شغّل **ملف الترحيل الجديد فقط**: `supabase/migrations/20260917100000_checkout_security.sql`، بعد الترحيـلات السابقة. لا تُعِد تشغيل `schema.sql` وحده؛ يحتوي تعريفات تاريخية قد تستبدل إصلاحات أحدث.
2. شغّل استعلامَي `supabase/diagnostics/checkout_security_verify.sql` كل واحد على حدة حتى تظهر نتيجتاهما. طابق تعريف الدالة مع الإصلاح، وتحقق من منع anon لاستدعاء إنشاء الطلب ومنع العميل من استدعاء مساعد الأسعار مباشرة.
3. جرّب طلباً بحساب تجريبي لمصنع معتمد، وتحقق من الأصناف والإجماليات. راجع حالات عدم تطابق الإجماليات إن ظهرت دون تعديل تلقائي للسجلات.
4. انشر الصفحات الثلاث المعدلة ضمن طريقة نشر الموقع المعتادة.

## ما لم يُحسم

- وصل تصدير حديث للدوال والسياسات والمشغلات كما هو موثّق أدناه، لكن لم تتوفر صلاحية اتصال إدارية مباشرة بالإنتاج. التصدير لا يحتوي جميع منح صلاحيات الجداول أو خصائص تفعيل RLS أو إعدادات Auth/Storage الكاملة، ولا يثبت مطابقة كل الترحيـلات.
- وصل تصدير أحدث لـSecurity Advisor يحتوي 39 تحذيراً، موضّحة أدناه. الحماية من كلمات المرور المسرّبة غير مفعّلة بحسبه؛ تظل حدود المعدل وCAPTCHA وإعدادات المصادقة الأخرى بحاجة إلى تحقق. لا تُسحب صلاحيات جميع دوال SECURITY DEFINER عشوائياً، فبعضها ضروري للتطبيق.
- صفحات الويب تحمل Supabase JS من CDN بوسم إصدار متحرك `@2` دون SRI، ودالة حذف الحساب تستخدم `npm:@supabase/supabase-js@2`. هذه فجوة في تثبيت الاعتماديات، وليست دليلاً على اختراق. لم يُغيَّر الإصدار دون اختبار توافق مستقل.
- لم تُفحص ترويسات استضافة الإنتاج، مثل CSP وHSTS ومنع تضمين الصفحات داخل إطارات، أو إعدادات TLS والنسخ الاحتياطية. لا يمكن استنتاجها من HTML المحلي.
- لم تُجر اختبارات اختراق حية أو ضغط أو تدقيق شامل لتطبيق Flutter المبني أو تاريخ الأسرار. فشل جلب تحديث Git بسبب صلاحيات `.git/FETCH_HEAD`، فالمراجعة تخص النسخة المحلية بدءاً من `cc30bae`.

## متابعة تصدير الإنتاج والتنبيهات الـ39

المصادر المقدمة من المالك: `Supabase Performance Security Lints (yhofxryhlrrwzztfowpa) (2).csv` بوقت رصد `2026-09-17T17:41:34.388Z`، ثم `Supabase Snippet Untitled query (6).csv` الذي يحتوي **46 دالة و72 سياسة و44 مشغلاً**. لم تُضف ملفات التصدير الخام إلى المستودع.

### تفسير العدد

- **11** تحذيراً لصلاحيات الزائر و**27** للمسجلين؛ تخص **27 دالة مختلفة**، لأن 11 دالة تظهر في المجموعتين.
- **1** تحذير لعدم تفعيل حماية كلمات المرور المسرّبة؛ تفعيلها من إعدادات Auth، وهي متاحة في Pro فما فوق وفق الوثائق الرسمية.
- المقارنة الفعلية بالتصدير `(1).csv` ذي 36 تحذيراً: اختفى تحذيرا `tier_unit_price`، وأُضيفت خمسة تحذيرات لـ`account_can_write` و`resubmit_factory_application` ودوال إنشاء/قبول/إغلاق العروض الخاصة. أي `36 - 2 + 5 = 39`؛ وبالمقارنة مع نتيجة المالك السابقة بعد إصلاح مساعد الأسعار، `34 + 5 = 39`.
- وجود تحذير على دالة الأعمال لا يثبت إمكان استغلالها. دوال المحادثات والطلبات تحتاج تنفيذ المسجلين مع التحقق من الهوية والملكية. دوال `is_admin` و`owns_factory` و`in_conversation` و`owns_cart` و`can_access_order` مستخدمة في سياسات الوصول؛ سحبها جماعياً قد يكسر القراءة.

### ما أكده التصدير من الإصلاحات السابقة

جسم `create_order_from_cart` يطابق ترحيل `20260917100000_checkout_security.sql` بعد تسوية الفراغات، وهو متاح للمسجل فقط. `tier_unit_price` و`has_purchased_product` و`account_deletion_media` غير متاحة مباشرة للزائر أو المسجل. مشغلات SECURITY DEFINER في التصدير كذلك مقفلة أمام الاستدعاء المباشر. توجد مشغلات تأكيد البريد/اعتماد الحساب على جداول الطلبات والرسائل والعروض الخاصة. هذه ملاحظات عن التعريفات والصلاحيات المصدرة، وليست اختبار اختراق حي.

### نتائج جديدة محددة

1. **كشف ملف عميل المحادثة:** السياسة `profiles_select_conversation_party` ما زالت موجودة في التصدير رغم وجود ترحيل محلي سابق يحذفها. تسمح لصاحب المصنع بقراءة صف عميل محادثته في `profiles`، بما يشمل البريد والهاتف وتاريخ الميلاد إذا كانت صلاحيات SELECT على الجدول متاحة. أُعيد إنتاج ذلك بالتعريفات المصدرة مع منح SELECT المعتادة وبيانات وهمية. التصدير لا يعرض منح الجداول نفسها. لا دليل من هذه المراجعة على حصول استغلال فعلي.
2. **كشف بيانات مصنع غير معتمد:** `get_factory_summary` تعمل بصلاحيات `postgres`، ومتاحة للزائر والمسجل، وتعيد المدينة وعدد المنتجات وحالة الاعتماد لأي معرّف دون فحص إتاحة المصنع. أُعيد إنتاج كشف بيانات مصنع pending/rejected مع حجب القراءة المباشرة تحت RLS. عولجت بتشغيل الدالة بصلاحيات المستدعي `SECURITY INVOKER`، لتتبع سياسات المصانع والمنتجات الموجودة مع إبقاء نفس التوقيع والأعمدة.
3. **خطأ في صلاحية رفع الشهادات:** الشرط المصدّر في `factory_certificates_storage_add` يقرأ `split_part(f.name, '/', 3)` بدل مسار الملف. أُعيد إنتاج رفض الرفع الصحيح باسم مصنع عادي، والسماح بمجلد مصنع غير موجود تحت مجلد المستخدم عند تشكيل اسم المصنع ليمر بالشرط. لا يتيح ذلك وحده انتحال هوية مالك آخر، ومشغّل ربط الشهادة يضيف فحصاً منفصلاً. الإصلاح يحدد `objects.name` صراحة.

### الإصلاح والاختبارات

الملف الجديد: `supabase/migrations/20260917120000_profile_factory_privacy.sql`، **أكد المالك نجاح تنفيذه بصورة «Success. No rows returned»، ثم أرسل `Supabase Snippet Untitled query (7).csv` وفيه الفحوص الستة جميعها `passed=true`. روجعت تفاصيل الشروط أيضاً؛ تجربة الميزات عملياً على الإنتاج لم تُؤكَّد بعد**. يحذف سياسة كشف الملف القديم ويضيف سياسة SELECT مقيّدة تحصر القراءة المباشرة بالنفس أو المدير حتى لو أُضيفت سياسة قراءة واسعة أخرى. تبقى دوال أسماء وصور أطراف المحادثات والتقييمات بصلاحياتها المحدودة، فلا تُفتح البيانات الخاصة. يشترط تفعيل RLS على الجداول المعنية، ولا يحذف سجلات أو ملفات، ولا يغيّر حالات الطلبات أو طرق الدفع.

اجتاز `scripts/test_profile_factory_privacy.mjs` تسع مجموعات بالنسخة المحلية ثم بالتعريفات والسياسات الفعلية في ملف `(6).csv`: إعادة إنتاج الحالات الثلاث، تكرار الترحيل وحفظ السجلات، عزل بيانات العميل والنفس والمدير ومنع ترقية الذات، استمرار أسماء المحادثات وآخر رسالة وعدّاد غير المقروء ونسبة التقييم لصاحبه وشارة الشراء، إخفاء المصانع غير المعتمدة عن الغرباء وإتاحتها لأصحابها والمدير، ضبط مسار رفع الشهادات وحواجز الاعتماد والبريد، والتحقق من رفض الترحيل عند تعطيل RLS. قاعدة الاختبار تعمل في الذاكرة فقط؛ لا اتصال أو قراءة أو تعديل لبيانات العملاء الفعلية. أضيف الاختبار إلى CI.

أكد تصدير `(7).csv` أن ملخّص المصنع يعمل بصلاحيات المستدعي مع بقاء استدعائه متاحاً للزائر والمسجل، وأن سياسة مشاركة الملف القديم أُزيلت، وأن شرط القراءة المقيّد هو النفس أو المدير. وأظهر شرط الشهادات المصحح `split_part(objects.name, '/', 3)` مع فحص مالك المصنع واعتماده وتأكيد الحساب، وتفعيل RLS ومنح SELECT للعميل على profiles/factories/products؛ السماح بـSELECT لا يتجاوز RLS. لم يحتوِ التصدير بيانات عملاء.

استعلام `supabase/diagnostics/profile_factory_privacy_verify.sql` يفحص الإعدادات فقط؛ بقيت تجربة المحادثات وعرض المصنع ورفع شهادة بحساب تجريبي. أرسل المالك بعده صورة Security Advisor: **صفر أخطاء و37 تحذيراً وصفر اقتراحات**، بانخفاض تحذيرين من39 يطابق تحوّل ملخّص المصنع إلى SECURITY INVOKER. الصورة تثبت العدد، ولا تعرض قائمة مفصلة جديدة للتنبيهات المتبقية. إصلاحا سياسة الملف الشخصي ومسار الشهادات لا يخفضان هذا العداد؛ انخفاض العدد ليس مقياساً كاملاً للأمان.

لا تثبت هذه المتابعة أن بقية الـ37 تحذيراً كلها آمنة، أو أن جميع السياسات والقنوات الأخرى خالية من الثغرات. يلزم التحقق من تفاصيل الإنتاج المتبقية وإعدادات كلمات المرور وحدود الطلبات والاستضافة؛ لم يُنفذ تغيير جماعي للصلاحيات لإخفاء التنبيهات.

## مصادر رسمية

- [صلاحيات ودوال Supabase](https://supabase.com/docs/guides/database/functions)
- [حماية الصفوف RLS](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [المفاتيح العامة والسرية](https://supabase.com/docs/guides/getting-started/api-keys)
- [حماية كلمات المرور](https://supabase.com/docs/guides/auth/password-security)
- [قاعدة OSV وواجهة الفحص](https://google.github.io/osv.dev/api/)
