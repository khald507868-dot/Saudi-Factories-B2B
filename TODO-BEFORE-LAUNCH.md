# قبل الإطلاق للمستخدمين الحقيقيين

## ✅ تأكيد البريد — مُفعّل (22 أغسطس 2026)

كان مُعطّلاً أثناء التطوير، فكان أي شخص يستطيع التسجيل
ببريد ليس له. وقد فُعّل من:
Supabase → Authentication → Sign In / Providers → `Confirm email`

وفي الجلسة نفسها انقلب `Allow anonymous sign-ins` إلى مُفعّل
سهواً ثم أُعيد إطفاؤه. **يجب أن يبقى مُطفأً** — فهو يمنح
الزائر صفة authenticated فتنفتح له سياسات RLS المخصصة للمسجّلين.

**ملاحظة:** الخطة المجانية ترسل عدداً محدوداً من رسائل التأكيد
في الساعة — تكفي للتجربة، وتحتاج خدمة بريد خارجية عند التوسّع.

## ✅ مسار التسجيل مع التأكيد — أُصلح في الكود (27 أغسطس 2026)

صار `handle_new_user` ينشئ بيانات الملف الشخصي والمصنع كاملةً
داخل الخادم من metadata التسجيل، ولم تعد صفحات التسجيل تحاول
تحديث صفوف RLS قبل وجود جلسة. عند غياب الجلسة تظهر رسالة تطلب
من المستخدم فتح رسالة التأكيد ثم تسجيل الدخول.

- [ ] تشغيل `fix-signup-email-confirmation.sql` في Supabase.
- [ ] اختبار حساب فرد جديد ببريد حقيقي.
- [ ] اختبار حساب مصنع جديد والتأكد من صف `factories` وحالة `pending`.

---

## بقية ما ينتظر

- [x] صفحة إدارة لاعتماد المصانع (status: pending → approved)
- [x] منح is_admin = true — عبر make-admin.sql (22 أغسطس 2026)
- [x] إضافة عمود industrial_license — add-industrial-license.sql (22 أغسطس 2026)
- [ ] نقل الصور من base64 إلى Supabase Storage
- [ ] جدول custom_prices للأسعار الخاصة بكل عميل
- [ ] حقل المنطقة والوصف في web-supplier.html (region_id فارغ حاليًا،
      فلا تجد خريطة home.html مصانع المنطقة)
- [x] .gitignore — موجود
- [ ] **نطاق حقيقي ورفع الموقع** — المشروع يعمل من file:/// فقط،
      فلا يصله أحد، واستعادة كلمة المرور لا تعمل بدونه
- [ ] بعد النطاق: تسجيله في Authentication → URL Configuration

---

## ما أُنجز (مُختبَر فعليًا)

- [x] قاعدة البيانات — 5 جداول
- [x] RLS — مُختبَرة بالهجوم لا بالمراجعة
- [x] إغلاق ثغرتَي تصعيد الصلاحيات (is_admin / status)
- [x] register.html — تسجيل حقيقي، تحقّق من المتصفح
- [x] login.html — دخول حقيقي: يرفض الخطأ ويقبل الصحيح

## المتبقي في المصادقة

- [ ] حماية الصفحات: زائر غير مسجّل يفتح home.html مباشرة
- [ ] زر تسجيل خروج في account.html
- [ ] factory.html يقرأ من قاعدة البيانات

## التحقق من السجل التجاري (مؤجَّل بقرار المالك)

لا يمكن التحقق آليًا من المتصفح — واجهة وزارة التجارة تحتاج
عقدًا رسميًا ومفتاحًا سريًا لا يجوز وضعه في صفحة HTML.

**الخطة المتفق عليها: مراجعة يدوية.**
المصنع يسجّل بحالة pending، والمالك يتحقق من رقم السجل
عبر الاستعلام العلني في موقع الوزارة، ثم يعتمد أو يرفض.

مؤجَّل حتى يُطلب:
- [ ] تحقق من الصيغة (10 أرقام تبدأ بـ 1 أو 2 أو 7)
- [ ] منع تكرار نفس السجل بين حسابين
- [ ] زر استعلام مباشر في صفحة الإدارة
- [ ] تحقق آلي عبر الوزارة (يحتاج عقدًا ورسومًا — عند التوسّع)
