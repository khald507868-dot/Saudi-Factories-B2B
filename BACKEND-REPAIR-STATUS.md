# حالة إصلاح الباك إند

## المنجز في فرع `fix/backend-foundation`

تم توحيد ترحيلات الأساس وقاعدة البيانات، وإضافة RLS للجداول الجديدة، وحماية صلاحيات المصنع والملف الشخصي والطلبات. أصبح حفظ المصنع عبر RPC ذرية داخل معاملة واحدة، كما أضيفت دوال خادمية للسلة والطلبات تعيد حساب الأسعار داخل PostgreSQL وتمنع التلاعب بإجمالي الطلب.

تم ربط حفظ الملف الشخصي ورفع صور المصنع إلى Supabase Storage، ومنع حفظ Base64 داخل بيانات تسجيل المصنع. أضيفت خدمة محادثات للرسائل النصية والمرفقات، مع روابط موقّتة للوسائط الخاصة وسياسة وصول تقصرها على أطراف المحادثة. كما أضيفت واجهتا السلة web وapp لقراءة عناصر السلة وإنشاء طلب بانتظار الدفع.

أضيف فحص ساكن محلي وGitHub Actions لفحص JavaScript والترحيلات عند كل Pull Request أو دفع إلى `master`.

## ما لم يُفعّل بعد

الدفع الإلكتروني الحقيقي غير مفعّل. طبقة `payment-provider.js` محايدة وتمنع أي محاولة دفع قبل اختيار Moyasar أو Tap وإنشاء Edge Function ووضع الأسرار في Supabase Secrets. لا ينبغي وضع المفاتيح في GitHub أو ملفات HTML.

## خطوات التطبيق الآمن

1. راجع التغييرات في الفرع وPull Request قبل الدمج.
2. طبّق ملفات `supabase/migrations/` على Supabase بالترتيب، أو استخدم أداة الترحيلات المعتمدة لديك. لا تعِد تشغيل ترحيلة سبق نجاحها إلا بعد مراجعة سجلها.
3. أنشئ حساب اختبار فرد وحساب مصنع، واختبر حفظ البيانات، فتح محادثة، رفع مرفق، إضافة منتج إلى السلة، وإنشاء طلب.
4. اختر مزود الدفع قبل بناء Edge Function الخاصة بإنشاء جلسة الدفع والـwebhook.
