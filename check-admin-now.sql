-- ============================================================
--  فحص: هل حسابي مدير فعلاً؟
--
--  زر "اعتماد" في صفحة إدارة المصانع لا يعمل إلا إذا كان
--  العمود is_admin = true في ملفك الشخصي. فإن كان false
--  فالحارس يُلغي التعديل بصمت — بلا رسالة خطأ.
--
--  استعلام واحد فقط، لأن محرّر Supabase يعرض نتيجة آخر
--  استعلام وحده — فلو وضعنا اثنين لاختفى الأول.
--
--  الخطوات: Supabase ← SQL Editor ← New query ← لصق ← Run
--
--  المطلوب:  is_admin = true
-- ============================================================

select email,
       account_type,
       is_admin,
       case when is_admin then 'مدير — سليم'
            else 'ليس مديراً — هذا سبب فشل الاعتماد'
       end as meaning
from public.profiles
where email = 'khald507868@gmail.com';
