-- ============================================================
--  فحص: هل صار حسابك مديرًا؟
--
--  يعرض كل الحسابات المسجّلة وحالة الإدارة لكلٍّ منها.
--  لا يعدّل شيئًا — قراءة فقط، آمن تمامًا.
--
--  كيف تشغّله:
--    Supabase -> SQL Editor -> New query -> الصق -> Run
-- ============================================================

select email,
       full_name,
       account_type,
       is_admin,
       created_at
from   public.profiles
order  by created_at;
