# تصحيح طلب المصنع المرفوض

تم تطبيق `20260914090000_factory_application_resubmission.sql` على مشروع `yhofxryhlrrwzztfowpa` بتاريخ 2026-09-14. التفعيل يضيف الدوال فقط، ولا يغيّر بيانات أي حساب أو مصنع قائم.

- تعرض صفحة `web-account-pending.html` زر «تعديل الطلب وإعادة إرساله» للحالة `rejected`.
- يحمّل النموذج بيانات المصنع الحالي: الاسم، السجل التجاري، الترخيص الصناعي، وتفاصيل العنوان.
- الدالة `resubmit_factory_application(details jsonb)` تستخرج هوية المالك من الجلسة، وتشترط بريدًا مؤكّدًا وحساب مصنع وطلبًا مرفوضًا. لا تقبل معرّف مصنع أو مالك من العميل.
- يسمح الخادم بحقول التسجيل المحددة فقط. النتيجة دائمًا `pending`، ويُمسح سبب الرفض القديم. تبقى موافقة المدير مطلوبة، وتبقى الكتابة في المنتجات والرسائل والتخزين محظورة أثناء الانتظار.
- لا يُنشأ حساب جديد ولا يتغيّر تأكيد البريد أو كلمة المرور. النموذج لا يعدّل البريد أو الهاتف أو الصور.
- أخطاء الإرسال تبقي المدخلات ليمكن إعادة المحاولة. تتوقف مراجعة الحالة الدورية أثناء تحرير الطلب حتى لا تمحو المدخلات.
- التغيير يخص نموذج الموقع؛ شاشة Flutter الحالية لا تحتوي نموذج إعادة الإرسال بعد.

## التحقق

نجحت اختبارات قاعدة البيانات المحلية باستخدام PGlite: منع غير المالك والحساب غير المؤكّد، رفض الحقول المحمية والقيم غير الصالحة، منع اعتماد الذات، بقاء الحظر بعد إعادة الإرسال، عدم تغيير البريد المؤكد، وعدم قبول إعادة إرسال طلب قيد المراجعة أو معتمد.

```sh
node scripts/test_factory_resubmission.mjs <directory-containing-node_modules>
```

اختُبر النموذج في Chrome ببيانات وهمية بالعربية والإنجليزية وبعرض ضيق: تحميل البيانات، الإلغاء، حفظ المدخلات عند الخطأ، الإرسال الصحيح، وظهور شاشة الانتظار. لم يُرسل أي طلب مصنع حقيقي في الاختبارات.
