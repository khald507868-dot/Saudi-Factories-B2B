# فحص أمني — 2026-08-28

فُحص بالهجوم الفعلي على الواجهة الحيّة، لا بقراءة السياسات.
المفتاح المستخدم هو المفتاح العام (المنشور)، أي بصلاحيات زائر.

## النتيجة: 13 هجومًا — كلها صُدّت

| # | الهجوم | النتيجة |
|---|---|---|
| 1 | قراءة `profiles` بلا حساب | `[]` صفر صفوف |
| 2 | قراءة `orders` بلا حساب | `[]` صفر صفوف |
| 3 | قراءة `messages` بلا حساب | `[]` صفر صفوف |
| 4 | اعتماد مصنع ذاتيًا `status=approved` | `[]` صفر صفوف |
| 5 | رفع النفس أدمن `is_admin=true` | `[]` صفر صفوف |
| 6 | كتابة رسالة في محادثة الغير | `42501` رفض |
| 7 | إنشاء طلب بسعر مزيّف `total=0.01` | `42501` رفض |
| 8 | تعديل سعر منتج الغير | `[]` صفر صفوف |
| 9 | قراءة سلال الآخرين | `[]` صفر صفوف |
| 10 | رفع ملف إلى Storage بلا حساب | `403` رفض |
| 11 | حذف ملف من Storage | `403` رفض |
| 12 | `save_factory_content` بلا حساب | `42501` رفض |
| 13 | رؤية المصانع غير المعتمدة | معتمد فقط |

**ملاحظة على قراءة النتائج:** بعض الهجمات تُرجع `HTTP 200` أو `204`
وهي رموز نجاح — لكن `[]` يعني **صفر صفوف تأثّرت**. لذلك استُخدم
`Prefer: return=representation` في كل هجوم كتابة: الرمز وحده يخدع.

## الدوال الخادمية

- 7 دوال `security definer`، وكلٌّ منها يثبّت `set search_path = public`
  مباشرة بعدها — يسدّ اختطاف المسار.
- `create_order_from_cart` **لا يقبل مبلغًا من العميل إطلاقًا**:
  الإجمالي `sum(quantity * price)` يُحسب داخل PostgreSQL، والمشتري
  من `auth.uid()`. التلاعب بالسعر ممنوع بنيويًا لا بسياسة.
- حمايات إضافية: رفض السلة الفارغة، سقف كمية 100000، منع خلط
  مصنعين في طلب واحد، و`idempotency_key` ضد الطلب المكرر.

## ديناميكية صفحات web

كلها تقرأ من قاعدة البيانات — لا مولّدات بيانات وهمية (فُحص).

| الصفحة | المصدر |
|---|---|
| `index` | `products` + `factories` |
| `web-factories` | `factories` |
| `web-factory` | `factories`/`products`/`posts` + `save_factory_content` + رفع |
| `web-product` | `products` (مع رسم أوّلي من localStorage ثم استبدال) |
| `web-cart` | `SFCommerce` كامل: تحميل/كمية/حذف/طلب |
| `web-messages` | `SFMessages` مع اشتراك لحظي — `threads` تُستبدل بالكامل |
| `web-account`/`settings`/`admin` | `factories` |
| `web-profile` | `profiles` + `factories` + رفع |
| `web-help`/`web-reset` | بلا قاعدة — مقصود |

## ربط الخوادم

ترتيب السكربتات سليم في كل الصفحات:
`i18n` ← CDN ← `supabase-config` (يعرّف `sb`) ← الخدمات ← `auth-guard`.
كل صفحة تنتظر `SF_AUTH_READY`، وكل خدمة ترفض الطلب بلا `SF_USER`.

توازن الأقواس: 14 صفحة web و8 ملفات JS — كلها سليمة.

## ملاحظات (ليست ثغرات)

- `select("*")` في `web-admin` و`web-factory`: جداول `factories`/
  `products`/`posts` عامة ولا تحوي أعمدة حساسة، والوصول محكوم بـ
  `is_admin()` أو RLS.
- الدفع الإلكتروني معطّل بقرار المالك — `payment-provider.js` يرفض
  أي محاولة، ولا يحمل مفاتيح. هذا مقصود.
- لا مفاتيح سرّية في أي ملف (فُحص `service_role` و`sb_secret`).
